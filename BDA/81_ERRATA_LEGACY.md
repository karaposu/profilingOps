# 81 · Errata — Known Wrong-As-Written in the Frozen Layer (and Uncorrected Finding Sentences)

> **Current as of:** 2026-08-25 · **Purpose:** the warning file for anyone reading `BASEFILES/profiling_data_extraction/pragma/` or the findings chain directly. The legacy layer is **frozen — never edited** — so its errors stand in place; findings are never retroactively edited either (the corpus killed that move four times). This file is where a reader learns what not to inherit. Each entry: where · what it says · what is actually the case · who corrected it.

## A · The frozen spec layer

**A1 · `core/new_apt_layer.md` — the formula, presented as current.**
Says: `Attachment ≈ f(charm, hope, fear) × g(non-extractive attention)` — three variables, one gate — while describing itself as "the current-state consolidated spec."
Actually: four variables and three gates since iteration-3.2. The file predates the Modulator Suite and was never migrated (≥6 of its own ten migration items unperformed; `MAGNITUDE`, `TYPE`, `Persistence`, `Coherence`, `Emotional Composure`, `Specificity` all zero-count in it).
Corrected by: `never_named_layer`, `model_completeness_fifth_category`; current formula in `10`.

**A2 · `core/new_apt_layer.md` — "Other known absences": Resonance listed as a "possible additional modulator."**
Actually: Resonance is the **fourth attachment variable** (additive — it cannot be a modulator, since it generates alone), admitted in iteration-3.2; the file beside it (`apt_should_be_extended_with_resonance.md`) says so. A reader trusting the main spec is told the theory has three variables and a speculative fourth; both statements are false.
Corrected by: iteration-3.2; `11` §5.

**A3 · `core/new_apt_layer.md` — Species × Attachment-Variable Amplification: "Mission-Focus amplifies Hope (followers hope to be part of the mission)."**
Actually: mission-participation fails Hope's sender-mediated clause; the component is **Resonance + access-Hope**. The same wrong claim stands in Scenario 9 (A4) — the two standing copies that motivated design rule 1. Note also the table's own oddest line — "Caregiver-Focus amplifies Charm via admiration (**others** admire the caregiving)" — is true but *object-axis* content wearing species clothing (the admirer is a third party; receiver-position matters).
Corrected by: `leadership_and_inexhaustibility`; `40` §3, `21` §2.

**A4 · `core/apt_layer_proof_scenarios.md` — Scenario 9's mechanism line ("Hope variable amplified particularly").**
Same error as A3, in the scenario file. The scenario's species reading and its observation stand; the variable attribution is wrong (Resonance did not exist in April — pre-paradigm addressing, not sloppiness).
Corrected by: same; restated correctly in `70` #9.

**A5 · `core/apt_layer_proof_scenarios.md` — Scenario 4's "Dialogue-Focus … or Caregiver-Focus."**
Not an error — a **standing unadjudicated fork**, genus-licensed for the scenario's own claim and decisive for any object-level question. Flagged here because a reader will not notice the "or" is load-bearing.
Recorded by: `artifacts_of_the_attention_species`; tracked as F2 in `80`.

**A6 · `core/apt_layer.md` — the May 2026 annotation block.**
The file's head carries an informal 7-line annotation (the belief-frame compressed) including a live question in the spec's own voice: "maybe we need a better name now since it is all related to belief?" — an open question sitting inside a frozen "current" spec, plus the rest of the file still describing the two-domain, three-variable stage.
Status: the annotation's content is superseded by `10`; the naming question is FR10 in `80`.

**A7 · Duplicate files.**
`old_apt_inference copy.md` is byte-identical to `apt_inference.md`; `old_apt_layer copy.md` is `apt_layer.md` minus the May annotation. Historical backups, no independent content.

**A8 · `core/pragma.md`, `apt_inference.md`, `apt_profiling.md`, `behavioral_profiling.md`, `interpretation_layer_*` — the instrument specs.**
All describe **two-domain, three-variable APT** (no Resonance, no Coherence/EC, no species/level vocabulary, no Specificity, no MAGNITUDE/TYPE). Not "wrong" for their date — but any implementation built from them alone inherits an abandoned formula. Current interface contract: `31`.

**A9 · `core/new_apt_layer.md` — the "Prescriptive Guidance Layer" heading.**
A section *heading* using the word "layer" over content that is neither a stage nor a part — a recorded instance of heading-does-not-confer-element-standing (`03` §2). The content itself is fine and is restated behind the banner in `60`.

**A10 · `core/apt_story_Example.md`.**
An unlabeled excerpt of copyrighted fiction (Sanderson, *Words of Radiance*) with no annotation of what it illustrates. Kept frozen as a curiosity (displayed-authority working short-term); pointer-only note in `70`.

## B · Finding sentences known wrong-as-written (their files stand unedited; corrections live downstream)

**B1 · `coherence_unpredictability_paradox` §3 + its third re-test:** "a reliable exploder's problem lives in Emotional Composure, not Coherence" — **over-assigns**. Deployed anger leaves EC intact; theatrical anger fails Coherence-Alignment; only uncontrolled spilling anger routes to EC. Corrected by `anger_routing` (and by the file's own prepended Corrections note — the one finding that carries one).

**B2 · `coherence_unpredictability_paradox` §5:** "fails generate-alone… same pair of criteria that excluded Vitality" — generate-alone is **not** a modulator criterion (it is the variable test), and Vitality was excluded on **one** ground (smoothness). The rejections themselves stand, on the correctly-cited criterion. Corrected by `model_completeness_fifth_category`.

**B3 · The earlier answer "deference that evaporates was never attachment" (quoted into `compliance_versus_attachment`):** right for the zero-Coherence case, wrong in its citation — it used the **nonzero-output Coerced row** to license a **zero-output** verdict, and the cited document uses "compliance" zero times. Corrected by `compliance_versus_attachment` §7.

**B4 · `compliance_versus_attachment` — "two independent derivations" of the two-family reading:** the variables and the type-rows are one object and its behaviour over time; the support is **internal consistency**, not independent corroboration. The result itself stands. Corrected by `forms_of_attachment` §7.

**B5 · `leadership_and_inexhaustibility` — "the corpus has no Task-Focus devotion case" (twice):** **Scenario 12 exists** (audiences "attach deeply" to performers absorbed in the music). Bounded correction: the *attachment* half is evidenced; the *followership* half remains the open test. Annotation recorded by `glue_versus_leader`; the sentence gains its Corrections note when that finding is next edited.

**B6 · `self_focus_generalization` (iteration-3) — the Mission-Focus demotion argument** ("collapses into Self-Focus"): reasoning **invalid** (a personal priority can be completed; a cause cannot; the exemplar sets separate cleanly). The demotion itself stands only because its promotion trigger demands empirical work that doesn't exist. Recorded by `leadership_and_inexhaustibility`.

**B7 · `apt_self_focus_reframe` (iteration-2) — "Self-Focus is narcissism's opposite":** retracted as a descriptive claim by iteration-3 (the mechanism is indifferent at first contact; sustainability differs). The prescriptive successor lives in `60`.

**B8 · The type-attribution tension inside `attachment_variable_interactions`:** the file calls the Fear-dominant case "a Fear-dominant attachment" in one section and withholds the word in the table cell "(relief, not attachment)" — the economical reading (the parenthetical describes the post-removal residue) is adopted by `30` §4, but the tension is real in the source.

## C · Reading rules for the legacy layer

1. Nothing under `BASEFILES/` reflects iterations 3.2+ unless it is the Resonance sidecar.
2. Any "current-state" self-description in a frozen file is false by construction now; the state record is this folder.
3. Quoted passages must be read to their end before use (`03` §7) — two reversals-in-the-next-sentence are on record.
4. Scenario-file claims carry April's vocabulary; check `70` for the corrected restatement and `03` §3.2 for the predication address before citing one.
