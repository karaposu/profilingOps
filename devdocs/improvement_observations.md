# Improvement Observations

Accumulated user observations about loop runs, per the /traverse post-completion protocol. When patterns emerge, run `/traverse "review improvement observations and propose spec changes"`.

## 2026-08-25 | leadership_as_calculation (can a technical person lead; is framework-as-calculation feasible; which gaps first) | 1 iteration | traverse

User feedback on the delivered finding (verbatim gist): *"here what it means term? make it more clear"* (on "runs Resonance-primarily on attention-direction toward a shareable term") and *"what catalog it refers to, make such things more clear, through the doc, reread it from scratch and detect ambiguous references and too compact meaning carrying parts and explain them better"* (on "the display catalog's own signal is comfort with silence").

**The pattern:** the finding passed CONCLUDE's self-run quality test yet still shipped ~26 instances of internally-referential shorthand — corpus vocabulary used without definition ("the term", "the display catalog", "Resonance", "backing", "Supplication", "bearing", "species", "outcome-independence", "PRAGMA"), corpus examples cited without introduction ("the calibrating performer"), and over-compressed multi-claim sentences. The author-side quality test ("can someone read ONLY finding.md?") is evidently too easy to self-pass when the author has been living in the corpus vocabulary all session — the exact failure mode CONCLUDE's non-ambiguity principle names.

**Fix applied this time:** a "The words this finding leans on" plain-language vocabulary section up front (12 entries) PLUS inline decompression at every first use, so sections read standalone; one-line descriptors for prior findings on first mention.

**Spec-change candidate (for when this recurs):** make a vocabulary-block + jargon-sweep an explicit CONCLUDE step for corpus-heavy findings — e.g., "list every term the finding uses that is defined only in another document; each must either get a plain-language definition block entry or an inline gloss at first use; corpus examples cited by shorthand get one descriptive clause." The self-test should be run against the *terms list*, not against a holistic re-read (the holistic re-read is what failed here — twice, since the glue finding from the same day likely carries the same defect family).
