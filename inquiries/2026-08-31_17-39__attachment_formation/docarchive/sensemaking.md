# Sensemaking — attachment_formation

## User Input

/Users/ns/Desktop/projects/profilingOps/inquiries/2026-08-31_17-39__attachment_formation/_branch.md + /Users/ns/Desktop/projects/profilingOps/inquiries/2026-08-31_17-39__attachment_formation/surfacing.md + /Users/ns/Desktop/projects/profilingOps/inquiries/2026-08-31_17-39__attachment_formation/articulate_warm.md

---

## SV1 — Baseline understanding

The user wants attachment *formation over time* expanded as a frontier field: a "generating" rate (the spoon) × duration; the conditions that form attachment beyond exposure (availability, common activity, need, hardship); the indirect route (bonds forming around a game or an exam, not toward the person); common fear as a fast route; per-person decay profiles; short vs long exposure regimes — all as frontiers, no premature definitions. First impression: the corpus has the rise-side law (beliefs accumulate from evidence) and a group-scale mechanism for the indirect route (co-attention → Resonance), but nothing about *rate*, nothing about the receiver's formation under shared objects, and no per-person profile; the "spoon" will probably decompose into quantities the corpus already has; the static bound will shape the deliverable into FR2's next customer.

---

## Phase 1 — Cognitive Anchor Extraction

### Constraints
- **C1 (the rise-side law):** f is cumulative and history-dependent; pre-loadable; strong priors move slowly; sub-threshold evidence does not update; independent sources update faster than correlated ones; every signal is scaled by its specificity (`10` §3–§4).
- **C2 (temporal precedence):** the gates read in seconds; f needs content to accumulate; in short windows g dominates because f has not developed (`21` §6).
- **C3 (the static bound):** the theory consumes levels; dynamics arrive "disguised as a missing modulator"; the decay-modulator was killed; FR2's customers *compose* static machinery; B5 forbids timeframes.
- **C4 (the object condition):** Resonance emerges when both parties hold the same attention-object; the Sid-function manufactures shared objects; a generative object keeps a relation open, an exhaustible one lets the model complete and pursuit extinguish (`11` §5; `40` §1, §4; `50` §2).
- **C5 (the keep-side):** the persistence column as maintenance-source × removal-event; Resonance internally maintained; expression waits for occasions; the record maintains Charm absent the person; revival at prior-strength.
- **C6 (inheritance):** the Fear variable is dread of a *sender*-mediated outcome × dependency; a shared third-party threat is not it; trauma bonding is adjacent.
- **C7 (scope):** beliefs are about the sender; receiver-side factors sit beside the formula (availability; the forming category of receiver-side scalers); per-person profiles live in `31`'s bearings.
- **C8 (the user's bound):** expand, branch, no premature definitions; application later.

### Key insights
- **K1 — "the spoon" decomposes into quantities the corpus already has.** Evidence delivered per unit time = occasions × specificity per occasion × cost visibly paid × independence of sources × (under perturbation, backing revealed). FR2 already names three of these as its dynamics parameters — **contact-frequency · context-breadth · perturbation-rate** — derived for detection-lag and frame-settling. The month in the military maxes all of them (24-hour contact; every context; constant perturbation; costs paid daily); four years of school runs at low values for long. The spoon is not a new parameter; it is the *evidence-delivery rate*.
- **K2 — the parameter-identity claim may extend a third time.** The corpus already claims that contact-frequency, context-breadth, and perturbation-rate govern *both* detection-lag (backing) and frame-settling-rate (window-close). If they also govern *formation-rate*, one triple governs three dynamics — the frontier's cleanest structural bet, and an observation, not a settlement.
- **K3 — the indirect route is the default because the direct route trips the gate.** Attention *on the relation* ("let's be friends"; "do you like me?") is attention on the counterparty's response — Supplication-shaped; attention *on a shared object* (the game) is non-extractive by construction. A shared object therefore does two things at once: it keeps Self-Positioning high (the gate) and it generates Resonance by co-attention (a variable). The kids' route is not a detour around attachment; it is the only route that does not collapse the gate.
- **K4 — Charm is not really an exception.** Charm's evidence also arrives through an object — the person seen *at their work* (demonstration, never description). What is "direct" about Charm is that the *belief* is about the person's standing; its *evidence* is still object-mediated. Every variable's evidence arrives through objects; every belief is about the person. The user's "unless it's the charm" dissolves into "Charm's object is the person's own work."
- **K5 — shared hardship is a shared object with stakes.** The exam, the enemy, the training month: a shared object (co-attention → Resonance) plus need (Safety-Hope, Exchange-Hope, Reciprocity-Hope — notes shared, help given) plus perturbation (backing shows under stress) plus costs paid at high specificity. It is not the Fear variable (nobody dreads the classmate) — it is the *fastest evidence-delivery configuration the corpus can describe*.
- **K6 — the exam is generative for exactly one season.** A shared object with a deadline exhausts; when it ends, the model completes and pursuit extinguishes — unless the co-attention has produced a shared model (Resonance) or been converted to a new object. That is why some exam-bonds persist (they became Resonance) and some dissolve (they were only need-Hope around an object that ended).
- **K7 — the four-year friend is FR3 in daily-life form.** "Can Transactional deepen to Bonded?" — the school friend starts as shared classes (Exchange-Hope around an object) and ends as a shared model (Resonance). A candidate mechanism: **conversion happens through the object** — a generative shared object, co-attended long enough, turns Exchange-Hope into Resonance.
- **K8 — the two decay profiles have two candidate mechanisms, both already in canon.** (i) *The mix:* hardship-bonds carry need-Hope (dissolves when the need ends — "dissolves when exchange ends") and Resonance (persists); a person's bad-time bonds persist to the extent they became Resonance rather than stayed need-Hope. (ii) *The bearing:* `31`'s triggers/blockers per variable — and one Hope flavor is *uniquely past-conditioned*: **Reciprocity-Hope** (a return on what I invested). The person who "doesn't care about the people who helped them" weights the past-conditioned flavor low and the present/future flavors high; the user weights it high. The decay profile may be exactly the receiver's weighting of past-conditioned against future-conditioned beliefs.
- **K9 — availability without contact is a keep-side channel with one formation face.** Reachability maintains Continuity-Hope (H_c) and lowers the cost of occasions; *known* availability ("they'd be there if I called") is Safety-Hope without any contact — a belief formed and maintained by the standing possibility, not by exchanges. Availability is mostly maintenance; its formation face is Safety-Hope.
- **K10 — the short regime is already lawful; the long regime is a composition.** Short exposure: g dominates (temporal precedence); f can be pre-loaded (social proof) and delivered at high specificity in one encounter (the demo on their case; costs visibly paid) — the spoon is maxed on specificity and cost because frequency is one. Long exposure: frequency and breadth do the work; consistency-accrual (Coherence, longitudinal); the ledger; inexhaustibility (bring new things) prevents the plateau. The docs already encode each regime.
- **K11 — the corpus's formation machinery is rise-side and sender-side.** Cumulative f describes the belief's rise; the five insistence mechanisms and wear-in describe the *sender's* sequence. Nobody has written the *receiver's* formation under shared objects — where the user's cases all live.
- **K12 — the theory was born as a retention question.** "How do you make someone keep talking to you"; the CAF dimension Continuity/Memory ("the arc of a relationship, not just a single exchange"). The static bound came later; formation dynamics is the founding question returning.
- **K13 — "spending time" is not one thing.** Duration with no occasions is nothing (absence); duration with occasions at zero specificity is correlated evidence (the same channel again — updates almost nothing: the insistence finding's discount); duration with new, specific, costly occasions is formation. Time is the container of occasions, not a variable.

### Structural points
- **S1** the evidence-delivery rate: occasions × specificity × cost × independence × perturbation — the spoon's decomposition, three factors already FR2's.
- **S2** the object's double job: gate (non-extractive attention) + variable (co-attention → Resonance) + deepening (generative vs exhaustible).
- **S3** the routes: object-mediated (default; all variables' evidence) vs relation-directed (gate-tripping).
- **S4** the shared-stakes configuration: object + need + perturbation + costs = the fastest formation.
- **S5** the two decay mechanisms: mix (need-Hope dissolves; Resonance persists) × bearing (past-conditioned vs future-conditioned weighting — Reciprocity-Hope).
- **S6** the regimes: short (g + pre-loading + one high-specificity encounter) vs long (frequency, breadth, consistency, the ledger, inexhaustibility).
- **S7** the container: FR2 (composition, never a modulator); FR3 (conversion through the object); B5.

### Foundational principles
- **P1:** formation is evidence-delivery; beliefs are about the person; evidence arrives through objects.
- **P2:** the gate reads in seconds and can be tripped at any point; no amount of f survives a collapsed gate.
- **P3:** properties are had; FR2's customers compose static machinery — no formation modulator.
- **P4:** structural-only — shapes and orderings, never rates or timeframes.
- **P5:** vocabulary-only external anchoring (mere-exposure, social-penetration, shared-adversity — precedent, never apparatus).

### Meaning-nodes
the spoon (evidence-delivery rate) · occasions · the shared object · co-attention · generative vs exhaustible · the gate's double service · shared stakes · the decay profile (mix × bearing) · Reciprocity-Hope's past-conditioning · availability (keep-side) · the two regimes · FR2/FR3 as container.

*Meta-inspection (H4, H5):* "evidence-delivery rate", "the object's double job", "shared-stakes configuration" are loop-coined — flagged; "the spoon" is the user's — to be decomposed, not adopted. The cases (the military month; the four-year friend; the kids; the exam group; the user's own profile) — flagged for specific-vs-pattern.

### SV2 — Anchor-informed understanding
The "spoon" is the corpus's own three dynamics parameters plus specificity and cost — an evidence-delivery rate, not a new variable — and the parameter-identity claim may extend to it. The indirect route is default because a shared object serves the gate and a variable at once; Charm's "exception" dissolves. Shared hardship is the fastest evidence-delivery configuration, not the Fear variable. The two decay profiles are a mix fact crossed with a bearing (Reciprocity-Hope's past-conditioning). Availability is keep-side with a Safety-Hope face. The regimes are already encoded. What the corpus lacks is the receiver's formation under shared objects — where all the user's cases live.

---

## Phase 2 — Perspective Checking

- **Technical / Logical.** Run the belief-frame literally on the military month: contact-frequency maximal (24 hours), context-breadth maximal (every context), perturbation-rate high (hardship), costs paid daily (sharing, covering), sources independent (many roommates, many situations) — evidence arrives at the highest rate the frame can describe, so f rises fast; and the shared object (surviving the month) is generative *for the month*, so the relation stays open; co-attention produces a shared model, which is internally maintained → persists at years. Four years of school: low perturbation, one context, moderate frequency, low cost — evidence arrives slowly but long; co-attention on shared objects (classes, games) produces the shared model eventually. **New anchor K14:** the same total f can be reached fast-and-perturbed or slow-and-calm, but the *mix* differs — the perturbed path over-weights backing evidence (Coherence read under stress; Safety-Hope) while the calm path over-weights Exchange-Hope and Resonance-by-accumulation. Formation *route* leaves a signature in the *type*.
- **Human / User.** The user's four cases each name a different formation configuration: the four-year friend (slow, calm, object-mediated), the military month (fast, perturbed, need-and-object), the kids (object first, attachment as by-product), the exam group (shared stakes, one season), and the user's own profile (keeps bad-time bonds; others keep fun-bonds). Their phrase "I did some things I would normally not do for other people" is the corpus's *costs paid* — the receiver's own costs, which the corpus models only on the sender's side. **New anchor K15:** costs paid by the *receiver* are evidence too — to themselves (self-signal: "I would not have done that for anyone") and to the other; a bond in which both pay is symmetric formation — the marriage doc's small daily costs generalized.
- **Strategic / Long-term.** If the parameter-identity extends to formation-rate, FR2 has a spine: one triple (contact-frequency · context-breadth · perturbation-rate) governs detection, settling, and formation — three dynamics from one set of levels, all composed without a modulator. For the user's application: the season's rule follows — a short exposure must max specificity and cost and pre-load through social proof; a long one must keep the object generative.
- **Risk / Failure.** (1) *Over-reading the spoon:* treating evidence-delivery rate as a term in f would be the decay-modulator's mistake in reverse — keep it as a description of how f's inputs arrive. (2) *The performed fast-bond:* intense short exposure can be manufactured (retreats, "bonding" exercises, love-bombing — intermittent reinforcement is adjacent); the backing finding says perturbation reveals backing, so a manufactured intensity without perturbation is a display; the display path is priced, not banned. (3) *Reading the military bond as dependency:* it looks Coerced-shaped (need; cannot leave) — but it persisted for years *after* the dependency ended, without relief — so it was not Fear-dominant; the survives-easing signature applies retroactively. (4) *Scope creep into a theory of memory:* "they will remember high school after ten years" is the persistence column's territory, already handled — formation and persistence must stay distinct.
- **Resource / Feasibility.** Everything is structural; the frontiers cost register lines; the one experiment-shaped item (the parameter-identity extension) is a stamped observation, blocked on data like its siblings.
- **Ethical / Systemic.** Manufactured shared adversity (hazing; cults' shared enemies) is the dark twin of K5 — shared stakes engineered by the sender; the honest/manufactured line from the docs applies to *objects* too: a real shared object vs an engineered one.
- **Definitional / Internal consistency.** (a) The object condition (`11` §5) is stated for Resonance; K3 claims it also serves the *gate* — is that a new claim? No: the genus is non-extractive attention, and attention on a shared object is non-extractive by definition; the two commitments were never joined. (b) "Evidence arrives through objects" for Charm — `11` §2's "Charm is carried only by specific signals; demonstration, never claims" is the same statement. (c) The user's "attachment forms indirectly" and the belief-frame's "beliefs are about the person" are compatible: route vs object of belief. (d) K8's Reciprocity-Hope reading rests on `11` §3's "uniquely past-positive-conditioned" — a committed asymmetry among Hope's flavors that has never been used; this is its first consumer.
- **Definitional / Frame-exit completeness** (gating fires: "time", "object", "cost" are inherited multi-value terms across committed structures). *Existence enumeration — "time":* duration (the user's); occasions (the charm_persistence finding); contact-frequency (FR2); temporal precedence's window (`21` §6); the ledger's cycle (the docs); persistence-through-absence (`30`). *Role assessment:* only *occasions* and *frequency* do formation work; duration is their container; absence is the keep-side's — re-locate "spending time" to occasions × frequency. *"Object":* the attention-object (`21` §2's species column); the shared term (`40`); the record (charm_persistence); the Sid-function's manufactured objects; the exam. All one referent-type (what attention is on) at different scales — no re-location. *"Cost":* the sender's costly signal (`21` §5); the receiver's costs (K15 — absent from canon); the docs' presence-at-cost. The receiver's costs are load-bearing for the military case → re-locate as a frontier (symmetric costs), not force into the sender-side account. *Verdict rigor:* strongest counter to "duration does no work" — mere exposure: familiarity alone attaches. Structurally: the belief-frame updates only on evidence; familiarity with no new specific evidence is correlated evidence — the corpus's discount says it updates almost nothing; what mere exposure does add is *occasions for expression* and lowered θ (threshold-crossing) — both already committed. Holds, with the threshold effect credited. *Residual:* none.
- **Phase / Calibration-state.** No rates; every frontier qualitative; the parameter-identity extension is the one falsifiable-in-expectation claim and is stamped as such.
- **The uncomfortable perspective (the spoon is a myth; attachment is exposure).** Mere exposure and proximity predict friendship in the field; the user's "spoon" may be storytelling after the fact. Under the frame: exposure delivers *occasions*; whether they carry evidence depends on specificity, cost, and perturbation — the military month and four years of school both had exposure, and the month formed more per day because its occasions carried more. The perspective lands one blow: *occasion density* (frequency) may do most of the work, with perturbation and cost as multipliers — the ordering of the factors is open.

*Meta-inspection (H1, H2, H3, H7):* H1 — the candidate set "spoon vs duration" collapses into "evidence-delivery (occasions × specificity × cost × independence × perturbation) inside duration"; two candidates become one composition. H2 — "time" and "cost" re-located (occasions; symmetric costs as a frontier). H3 — the user's framing ("generating variable and depth variable") presupposed two parameters; the reframed question is *what delivers evidence, and how fast*. H7 — no calibration.

### SV3 — Multi-perspective understanding
Formation = evidence delivery through shared objects. The spoon is the delivery rate (occasions × specificity × cost × independence × perturbation — three of which are FR2's own parameters); duration is the container of occasions; the route is object-mediated by default because a shared object keeps the gate open while generating Resonance; shared stakes are the fastest delivery configuration and not the Fear variable; conversion (Transactional → Bonded) happens through a generative object; the decay profiles are the mix (need-Hope dissolves; Resonance persists) crossed with a bearing (the weight on past-conditioned Reciprocity-Hope); availability is keep-side with a Safety-Hope face; the receiver's own costs are evidence the corpus has not modeled; the regimes are already encoded; the container is FR2, entered by composition.

---

## Phase 3 — Ambiguity Collapse

#### A1 · "The spoon" — a new parameter, or a composition of committed ones?
**Strongest counter-interpretation:** the spoon is a real single quantity — *intensity* — that the corpus lacks and should name.
**Why it fails (structural):** every candidate content of "intensity" is already a committed quantity: specificity (per signal), cost paid (per display), contact-frequency, context-breadth, perturbation-rate (FR2's triple), source independence (`10` §3). A new term would duplicate them and revive the decay-modulator's error (a dynamics quantity admitted as an element). The counter keeps a point: their *composition* — product, threshold, order — is unwritten.
**Confidence:** HIGH. **Resolution:** the spoon = the evidence-delivery rate, a composition of committed quantities; the composition is the frontier. **Fixed:** no new term. **No longer allowed:** "intensity" as an element. **Depends:** every parameter frontier. **Model change:** the user's two-parameter picture becomes "delivery rate inside duration."

#### A2 · Why formation is indirect — the gate explanation
**Strongest counter-interpretation:** indirectness is just how children socialize; adults form attachments directly (conversation, dating).
**Why it fails (structural):** adult direct approaches are the corpus's own worst cases (the permission-seeking template; the chaser) — attention on the relation is attention on the response, Supplication-shaped; the approaches that work are object-anchored (a specific interest, a demo on their case, "attention beats declarations"). The counter keeps a point: adults *can* make the relation itself an object of dialogue (Dialogue-Focus on "us") — which is still an object.
**Confidence:** HIGH. **Resolution:** the indirect route is default because a shared object keeps the gate open while delivering evidence; the direct route trips the gate. **Fixed:** the object's double job. **No longer allowed:** "children are special." **Depends:** A3, A7. **Model change:** the object condition joins the gate condition — one structure, two committed halves.

#### A3 · "Unless it's the charm" — is Charm the direct exception?
**Strongest counter-interpretation:** yes — we admire a person directly; no shared object is needed.
**Why it fails (structural):** Charm is carried only by specific signals — demonstration, never claims; the admirer sees the person *at their work* (the object). The belief is about the person; the evidence arrives through the person's own object. **Confidence:** HIGH. **Resolution:** Charm's object is the person's own work; not an exception to object-mediation, an instance. **Fixed:** all four variables' evidence is object-mediated. **Model change:** none beyond dissolving the exception.

#### A4 · Common fear — the Fear variable, or shared stakes?
**Strongest counter-interpretation:** the exam group *does* run on Fear — the teacher can fail them; the fear is of a sender.
**Why it fails (structural):** the Fear variable is a belief about the *attached-to* person (the classmate), who threatens nothing; the teacher is a third party who is not the object of the bond. What the bond runs on: a shared object (the exam) + need (Safety/Exchange/Reciprocity-Hope: notes, help) + perturbation (backing revealed) + costs paid — the fastest delivery configuration. The counter keeps a point: the *teacher* may gain a Fear-dominant attachment from the students — a different dyad.
**Confidence:** HIGH. **Resolution:** shared stakes, not the Fear variable; the fear_as_outcome inheritance holds. **Fixed:** the shared-stakes configuration. **No longer allowed:** "fear forms attachment" without the dyad named. **Depends:** A5 (why some such bonds dissolve). **Model change:** none — inheritance applied.

#### A5 · The two decay profiles — mix or bearing?
**Strongest counter-interpretation:** purely the mix — hardship-bonds are need-Hope and dissolve when the need ends; no personal trait needed.
**Why it partially fails (structural):** the mix explains *which* bonds dissolve (need-Hope) and persist (Resonance) but not why *the same* bad-time bond persists for the user and dissolves for the other person; the residual is receiver-side — how each weights the one past-conditioned Hope flavor (Reciprocity: "a return on what I invested" — `11` §3, "uniquely past-positive-conditioned") against future-conditioned flavors, and which objects their Resonance forms around (struggle vs fun). Both mechanisms are needed.
**Confidence:** MEDIUM-HIGH. **Resolution:** the decay profile = the mix (what the bond was made of) × a bearing (the receiver's weighting of past-conditioned vs future-conditioned beliefs, and their Resonance-forming objects) — the bearing's home is `31`. **Fixed:** two mechanisms, both canon-grounded. **No longer allowed:** a "gratitude trait" as a new element. **Depends:** the bearings frontier. **Model change:** Reciprocity-Hope's past-conditioning gets its first use.

#### A6 · Availability without contact — formation input or maintenance channel?
**Strongest counter-interpretation:** a formation input — knowing someone is reachable builds attachment on its own.
**Why it partially fails:** without occasions nothing is evidenced; what availability does is keep Continuity-Hope alive and lower the cost of the next occasion (maintenance) — *except* that known availability is itself a Safety-Hope belief ("they'd be there"), formed by the standing possibility. **Confidence:** MEDIUM-HIGH. **Resolution:** availability is keep-side (maintenance of H_c; cheaper occasions) with one formation face (Safety-Hope). **Fixed:** the placement. **Model change:** the conditions catalogue gains a keep/form distinction.

#### A7 · Deepening — FR3 through the object
**Strongest counter-interpretation:** deepening is just more of the same beliefs (higher f), not a change of kind.
**Why it fails (structural):** the type table's rows differ in *persistence*, and the four-year friend's bond persists through absence (Bonded), which the early shared-classes bond (Transactional) would not — the kind changed; the corpus's own FR3 asks how. The mechanism available: co-attention on a generative shared object accumulates a shared model — Exchange-Hope around the object becomes Resonance with the person. **Confidence:** MEDIUM (a candidate mechanism for an open frontier). **Resolution:** conversion through the object, posed as FR3's candidate. **Fixed:** the frontier's wording. **Model change:** FR3 gains a mechanism candidate.

#### A8 · The static bound — can formation be formalized inside BDA?
**Strongest counter-interpretation:** yes, as a formation modulator or a rate term.
**Why it fails (structural):** the decay-modulator kill (criterion-loosening); FR2's rule that dynamics arrive disguised as modulators; the insistence precedent (compose cumulative f + longitudinal Coherence + decay). **Confidence:** HIGH. **Resolution:** formation is composed from static machinery; the frontier field is FR2's customer, with the parameter-identity extension as its stamped observation. **Fixed:** no modulator; no term. **Model change:** FR2 gains a formation-side customer and a candidate spine.

#### A9 · The receiver's own costs
**Strongest counter-interpretation:** costs are sender-side signals only; what the receiver pays is irrelevant to their beliefs about the sender.
**Why it fails (structural):** the user's "I did things I would not do for others" is a self-observed cost that updates the receiver's *own* model of the bond (a belief about the pair — the frame layer's pair-frame) and, when seen by the other, is a costly signal to them: in a symmetric bond both are senders. **Confidence:** MEDIUM. **Resolution:** symmetric costs — each party's paid costs are evidence to the other and to themselves — a frontier, placed beside the pair-frame. **Fixed:** the frontier. **Model change:** formation in dyads is two-directional evidence.

#### A10 · Load-bearing concept test — the coinages
**Resolution:** "evidence-delivery rate" is a gloss for the composition (the user's word "spoon" and the corpus's three parameters carry); "the object's double job" is a gloss for the joined commitments; "shared-stakes configuration" is a gloss. None enters canon as a term. **Confidence:** HIGH.

#### A11 · Specific-vs-pattern
**Strongest counter-interpretation:** the account fits friendship and hardship but not romance or sales.
**Why it holds anyway:** romance's shared objects are the dates' activities and the pair itself as a Dialogue-object; sales' object is the product on the customer's case; the regimes differ in delivery rate, not in mechanism. **Confidence:** HIGH. **Resolution:** the pattern is general; the cases are illustrations; the regimes are the pattern at two delivery rates.

#### A12 · Deliverable shape
**Resolution (as in the fear inquiry):** a frontier field registered under FR2/FR3 as sub-questions; fixed points as observations; nothing settled. **Confidence:** HIGH.

*Meta-inspection: H8 — external reference points used: the user's testimony (the military month; the user's own profile); the docs' field-derived rules; vocabulary-only precedents (mere exposure; shared adversity) named as precedent only.*

### SV4 — Clarified understanding
Clear: the spoon is a composition of committed quantities (three of them FR2's); duration is the container of occasions; the indirect route is default because the shared object keeps the gate open while delivering evidence; Charm's exception dissolves; shared stakes are the fastest delivery configuration, not the Fear variable; deepening is FR3 through a generative object; the decay profiles are mix × bearing with Reciprocity-Hope's past-conditioning as the bearing's likely content; availability is keep-side with a Safety-Hope face; the receiver's own costs are unmodeled evidence; formation is composed, never a modulator. No longer viable: "intensity" as an element; a formation modulator; the Fear variable as the exam-bond's engine; a gratitude trait as an element; "spending time" as a variable.

---

## Phase 4 — Degrees-of-Freedom Reduction

**Fixed (as observations):**
1. Formation = evidence delivery; the spoon = delivery rate = occasions × specificity × cost × independence × perturbation; duration = the container.
2. Three of the rate's factors are FR2's dynamics parameters; the parameter-identity claim may extend to formation-rate.
3. The object's double job: gate + variable; the indirect route is default; Charm's exception dissolves.
4. Shared stakes = object + need + perturbation + costs — the fastest configuration; not the Fear variable.
5. Deepening = conversion through a generative object (FR3's candidate); exhaustible objects plateau.
6. Decay profiles = mix × bearing; Reciprocity-Hope's past-conditioning as the bearing's content; home `31`.
7. Availability = keep-side with a Safety-Hope face.
8. Symmetric costs — the receiver's paid costs as evidence — unmodeled.
9. The regimes are already encoded (temporal precedence; cumulative f + consistency + the ledger + inexhaustibility).
10. Composition, never a modulator; FR2 as container.

**Eliminated:** "intensity" as an element · a formation modulator or rate term · timeframes · the Fear variable as the exam-bond's engine · a gratitude trait as an element · "spending time" as a variable · a theory of memory (persistence stays separate).

**Viable frontiers remaining:**
- F-1 the delivery-rate composition (product / threshold / order; which factor dominates — occasion density vs perturbation).
- F-2 the parameter-identity extension to formation-rate (a stamped observation; what would test it).
- F-3 the object's double job at dyad scale (the Sid-function without a Sid; the relation-as-object case).
- F-4 the shared-stakes configuration (why fast; which variables; the one-season object; the honest/manufactured line for objects).
- F-5 conversion through the object (FR3's candidate mechanism; what converts, what plateaus).
- F-6 the decay-profile bearing (past-conditioned vs future-conditioned weighting; Resonance-forming objects; a persistence dimension for `31`).
- F-7 availability's two faces (maintenance of H_c; Safety-Hope by standing possibility).
- F-8 symmetric costs (the receiver's paid costs as evidence; the pair-frame).
- F-9 the route's signature in the type (fast-perturbed vs slow-calm formation leaving different mixes).
- F-10 the regimes' movable parameters (the sales case; the restaurant case) — application, deferred.
- F-11 the retention question restored (the origin's founding question as FR2's motivation; Continuity/Memory).

### SV5 — Constrained understanding
One account with no new elements: **attachment forms as evidence is delivered through shared objects** — at a rate composed of quantities the corpus already has — and the object serves the gate and the variables at once, which is why formation is indirect, why shared stakes are fast, and why deepening is conversion through a generative object. Eleven frontiers open from it; the container is FR2/FR3; nothing is settled.

---

## Phase 5 — Conceptual Stabilization

**Coherent interpretation.** The user's two-parameter intuition (a generating rate × exposure time) is right in shape and reducible in content: the rate is evidence delivery — occasions, specificity, cost, independence, perturbation — three of which the corpus already named as its dynamics parameters; time is the container of occasions. The indirect route the user observed in children is the theory's default route for everyone, because a shared object keeps the gate open while generating Resonance; Charm's apparent exception is the same route with the person's own work as the object. Shared hardship is the fastest configuration the frame can describe and not the Fear variable. The four-year friend is FR3 — conversion through a generative object. The two decay profiles are a mix fact crossed with a receiver bearing whose likely content is the weight on the corpus's one past-conditioned Hope flavor. Availability is keep-side with a Safety-Hope face. The receiver's own costs are evidence the corpus never modeled. The regimes are already encoded. All of it composes static machinery — the frontier field is FR2's next customer.

**Problem structure.** one account (delivery through objects) · one composition (the rate) · one joined commitment (the object's double job) · one configuration (shared stakes) · one conversion mechanism (FR3) · one profile structure (mix × bearing) · eleven frontiers.

**Action framework.** Register the field under FR2/FR3 as sub-questions; record the fixed points as observations; stamp the parameter-identity extension as a candidate; leave every frontier branchable.

*Accommodation check (H6):* the model settled after Phase 2; the uncomfortable perspective (mere exposure) became a factor-ordering question inside F-1, not an exception. No misfit.

### SV6 — Stabilized model

> **Attachment forms as evidence is delivered through shared objects.** The rate of delivery — what the user called the spoon — is occasions × specificity × cost × independence × perturbation, three of which the corpus already names as its dynamics parameters (contact-frequency · context-breadth · perturbation-rate); duration is only the container of occasions. A shared object does two jobs at once — it keeps the gate open (attention on the object is non-extractive) and generates Resonance by co-attention — which is why formation is indirect by default, why shared stakes bond fastest (object + need + perturbation + costs), and why deepening is conversion through a generative object while exhaustible objects plateau. Charm is not an exception: its object is the person's own work. The two decay profiles are what the bond was made of (need-Hope dissolves; Resonance persists) crossed with how the receiver weights past-conditioned against future-conditioned beliefs. Availability is keep-side with a Safety-Hope face; the receiver's own paid costs are unmodeled evidence; the short and long regimes are already encoded. Nothing here is a new element; eleven frontiers open; FR2 and FR3 are their container.

**How SV6 differs from SV1.** SV1 expected to find a missing rate parameter and a receiver-side gap. SV6 finds that the rate is a composition of things the corpus already has (and may unify three dynamics under one triple), that the *object* — not time — is formation's unit, that the gate and the variables are served by the same object, that the exception the user allowed for Charm dissolves, and that the decay-profile question has two canon-grounded mechanisms including a Hope flavor never used before.

## Saturation telemetry
- Perspective saturation: five perspectives produced new anchor types (K14 route-signature in type; K15 symmetric costs; the frame-exit re-locations; the gate-join; the mere-exposure ordering); the last two confirmed → approaching saturation.
- Ambiguity resolution: 12/12 (A5, A6, A7, A9 at MEDIUM / MEDIUM-HIGH). OPEN by design: F-1–F-11.
- SV delta: large (a missing parameter → objects as formation's unit; a composition; a joined commitment).
- Anchor diversity: constraints 8 · insights 15 · structural 7 · principles 5 · meaning-nodes 12; seven perspectives.
- Failure modes: status-quo bias (the static bound was not protected — it was *used*; the user's two-parameter picture not protected either); anchor dominance (K1 is strong — checked against K3, K5, K8, K15: several pillars); clean-resolution trap (counters carried, several keeping a point); premature stabilization (absorbed, not patched); self-reference (external points named).
