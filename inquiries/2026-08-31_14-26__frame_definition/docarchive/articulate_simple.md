# Articulate-Simple — frame_definition

## User Input

```text
[The full spoken transcript, preserved verbatim — the BDA-usability preamble ("we want to be able to use it basically anywhere"; the principles-docs as one attempt; "we cannot operate such way. We are not computers"); the frame as the compact concentrated version people actually run ("submit to a frame… wear a frame on top of their social presence… try to keep maintain that frame… not broken… this frame is what leads them to do correct actions"); the dressing example (the uncomfortable-but-sharp outfit as power vs the comfort-frame man — "neither of them are actually wrong… depending on other things"); the asks ("What is a frame? How it is relevant to BDA. And how it is related to us. Is it something like a compact knowledge or is it…"); assumability ("instead of memorizing all of this BDA theory details, I can just assume a frame, and that frame should be assumable. It's not just information"); the coffee-shop example ("I am the most important person in this coffee shop… the Barista should take care of me… this is a frame, for example, and it works"); and the explicit deferral ("my goal, not now, but later on, is to establish what this BDA theory defines as the best frame… But for now, let's focus on what is the frame.")]

Transcription-noise register: "belief-triven" = belief-driven; "valved" ≈ valued.
```

*(The verbatim text lives in full in `_branch.md`'s Source Input; both records are authoritative.)*

## Statement-Level Fields

- **Itemize count:** 1
- **Per-item identifiers:** `frame-definition`
- **Substrate:** WARM — the corpus is session context, and it already uses "frame" in multiple committed senses; the input invokes BDA by name and positions the frame as the theory's usable runtime form. Keep-together: one item (the definition); **the best-frame question is explicitly deferred by the user** ("not now, but later on") — it is the named successor, not an item.

---

## Item 1 — `frame-definition`

**Item text:** Define the frame: what it is, how it relates to BDA and to us, and whether it is compact knowledge or something else — such that it is *assumable* ("not just information") and can later serve the deferred best-frame question.

### MQ1 — verdict-axis
**Q:** What is the user asking for?
**A — identified-ambiguities-list:**
- `[definition-kind]` — three deliverable-kinds gestured: an **ontology** (what kind of thing a frame is — knowledge / premise-set / state / policy), a **BDA-mapping** (which committed objects compose it), and a **functional account** (what a frame *does* — compress the theory into something runnable without active thinking). All three textually present.
- `[frame-term-collision]` — the corpus already holds **committed frame-senses**: (a) **frame-consent / context-frames** (theater, role-expected warmth; the context-nulls of the window-close account); (b) **the pair-frame** ("what we are" — the settling object); (c) **the Expressed Frame** from the SP lineage ("I'm the selector" vs "please like me") plus the approach doc's frame-clarity axis. The user's sense — the *worn self-frame that generates conduct* — is a fourth usage-candidate; the inquiry must sort the collision (map / extend / name distinctly).
- `[scope-of-the-frame]` — the examples span a *per-context assumed premise* ("most important person in **this** coffee shop"), a *standing personal stance* ("I never submit to dress codes"), and *situational choices* (the outfit): is the frame per-context, standing, or layered?
- `[assumability]` — "I can just **assume** a frame… it's not just information": what makes something assumable rather than merely known? (The corpus's state-manufacture machinery is the obvious candidate; whether it is the right one is the question.)

### MQ2 — context-need axis
**Q:** What context does the response need that isn't in the statement?
**A — identified-ambiguities-list:**
- **verdict:** the corpus's frame-senses (frame-consent; context-nulls; the pair-frame's **stance-bundle** definition — *label + expected conduct + claim-scope*; the **Expressed Frame** — "I'm the selector," which is the coffee-shop frame's committed ancestor); **the calculation program** (bearing — "the stable posture that becomes part of what others call character"; the card; *posture-program, not display-manual*; state-cultivation → conduct emerges — the already-committed answer to "we can't run rules in the moment"); **`60` §5 Stanislavski** (assuming = manufacturing the real state; the presence-path); **`21` §8** (the standing self-claim — the coffee-shop frame reads as a *self-claim assumed for a context*); **backed vs performed** (a worn frame either manufactures the state or performs it; the arc prices the second); **Coherence** (frame-maintenance — the user's "try this frame to be not broken" — as the coherence gate's daily face; frame-breaks as re-pricing events); the **species-context-fit table** (`60` §2 — the two dressers' "neither is wrong" has a committed home: context-fit plus what the person is "hoping from that meeting"); the **compressed cards** in the principle docs (the existing half-way compression objects); the user's phrase "**submit to a frame**" (submission to one's *own* frame — a self-imposed conduct-generator).
- **kinds:** a **frame** = `[a premise-set held / an assumed self-claim + stance-bundle / a manufactured state / a compressed policy — or a composition]`; **wearing** = `[holding a premise / manufacturing a state / performing a display]`; **broken** = `[contradicted by one's own conduct / successfully challenged by another / dropped under load]`.
- **stance:** meaning-layer fundamentals (what the thing IS), with the best-frame adjudication **deferred to the named successor**; the definition must expose the successor's interface (the parameters a best-frame question would optimize). Structural-only.
- **transcription-noise register:** as above.

### MQ3 — intent-axis (WHAT)
**Q:** What is the user trying to accomplish?
**A — identified-ambiguities-list:**
- `[define-the-frame]` — the ontology committed;
- `[sort-the-term-collision]` — the four senses reconciled into one family;
- `[map-to-BDA]` — the composition named from committed objects;
- `[adjudicate-assumability]` — what makes a frame wearable rather than memorizable;
- `[explain-the-examples]` — the coffee-shop frame and the two dressers in theory terms;
- `[set-up-the-successor]` — the definition shaped so the best-frame inquiry can consume it.

### MQ4 — boundary-axis
**Q:** What is the user explicitly excluding?
**A — identified-ambiguities-list (NOT-list):**
- **Not the best-frame adjudication** — the user's own words: "not now, but later on… for now, let's focus on what is the frame." The successor is set up, never run.
- **No external frame-control literature as ground** — NLP/pickup "frame" doctrine stays out; folk/field terms as illustration only (the standing bound).
- **Structural-only** — no empirics.
- **The coffee-shop example is an example, not an endorsement** — the account explains *why it works* in theory terms and may note its edges (the entitlement-frame's costs) without moralizing; the user said "it works," and the analysis owes them the mechanism, not a lecture.

### MQA
**reconcile** — three joints:
1. **The collision-sort is primary:** four committed senses already exist; the definition must land *inside the family* coherently (the senses may turn out to be one object viewed from different sides — context-frames, pair-frames, and self-frames all being stance-bundles at different objects).
2. **Assumability and the ontology resolve together:** if a frame is a premise + state bundle, assumability *is* state-manufacture (the Stanislavski machinery — committed); if a frame were mere knowledge, assumability would be unexplained — the user's own "not just information" points at the state-reading, but the pipeline must earn it, not inherit it.
3. **The deferred successor shapes the deliverable:** the definition must expose what a best-frame question would vary (the frame's content, scope, backing-requirement, context-fit) — an interface, not an answer.

### Deconstruct
**Tuple:** (deliverable: the frame definition — the term-collision sorted, the ontology committed, the BDA-composition named from committed objects, assumability adjudicated, both examples explained, the successor's interface exposed; kinds: meaning-layer conceptual analysis through the full pipeline into a finding, then homing routes; bounds: best-frame deferred, no external frame-literature ground, structural-only, examples explained not endorsed).
**Late-split check:** one item; the examples are applications; the successor is deferred by the user's own instruction. No late-split.

### MultiDepth
**Literal-statement:** "We built BDA to use it basically anywhere; the principles were one attempt, but we cannot operate by memorizing — we are not computers. What people actually run is a *frame*: a compact, concentrated version they submit to, wear over their social presence, maintain unbroken — and it leads them to correct actions. What is a frame? How is it relevant to BDA and to us? Is it compact knowledge or something else? It should be *assumable* — not just information ('I am the most important person in this coffee shop' — and it works). Later we will establish what BDA defines as the best frame; for now: what is the frame?"
**Identified-purpose-motivation-ambiguities (WHY-axis):**
- `[usability]` — the theory's runtime form: humans can't execute principle-lists in the moment;
- `[compression]` — many principles → one wearable object;
- `[naturalness]` — people already operate this way (parent-taught, folk-learned); the theory should *name what people actually do*;
- `[successor-setup]` — the best-frame question is the declared destination; the definition is its prerequisite.

### Considered Articulations
1. "Sort the collision: the corpus's four frame-senses (context-frames/frame-consent; the pair-frame; the Expressed Frame; the worn self-frame) — one family or distinct objects, and where the user's sense lands."
2. "Commit the ontology: is a frame knowledge, a premise-set, an assumed self-claim + stance-bundle, a manufactured state, a compressed policy — or a specific composition of these?"
3. "Name the composition from committed objects: standing/assumed self-claim (`21` §8), stance-bundle (label + expected conduct + claim-scope), bearing (`60` §8), the manufactured state (`60` §5) — what exactly assembles into 'a frame'?"
4. "Adjudicate assumability: what makes a frame *wearable* rather than memorizable — the state-manufacture reading of 'assume' versus a premise-holding reading — and what the backed/performed machinery says about worn frames."
5. "Explain the examples and expose the successor's interface: the coffee-shop frame and the two dressers in theory terms (context-fit; what each is 'hoping from that meeting'); the parameters the later best-frame inquiry will optimize."

---

## Self-Assessment

LAYER 1 self-check (single LIGHT pass): Mode 1 no-fire (keep-together held; the successor deferred per the user's own instruction, not dropped) · Mode 2 no-fire · Mode 3 no-fire · Mode 4 no-fire · Mode 5/6 no-fire (MQ2 carries verdict/kinds/stance + the noise-register) · Mode 7 no-fire (2-shape held) · Mode 8 no-fire · Mode 9 no-fire (five variants span collision / ontology / composition / assumability / examples+interface within the NOT-list).

Zero fires; low friction.

**Verdict: HIGH-PROCEED**
