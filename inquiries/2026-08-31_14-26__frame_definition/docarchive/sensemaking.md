# Sensemaking — frame_definition

## User Input

/Users/ns/Desktop/projects/profilingOps/inquiries/2026-08-31_14-26__frame_definition/_branch.md + /Users/ns/Desktop/projects/profilingOps/inquiries/2026-08-31_14-26__frame_definition/surfacing.md + /Users/ns/Desktop/projects/profilingOps/inquiries/2026-08-31_14-26__frame_definition/articulate_warm.md

## Cognitive Anchors

- **A1** the four frame-senses + the pair-frame's committed **stance-bundle** definition (label + expected conduct + claim-scope).
- **A2** the calculation program — the founding constraint ("displays can't be chosen in the moment"), the posture-answer, **bearing** as endpoint, the card.
- **A3** **state co-generation** — a real state produces words, timing, posture, and choices for free; composition costs bandwidth.
- **A4** `60` §5 Stanislavski — manufacture the real state on demand; the presence-path.
- **A5** **premise-posture** — the opening frame of one's moves (the catalog's own word).
- **A6** backed vs performed wearing — the arc prices the performed mode.
- **A7** `21` §8 — the standing self-claim; claim-scope (occurrent / context-scoped / standing).
- **A8** wearing-as-formation — micro-declarations grain both ledgers; the presence-path's unit.
- **A9** "submit to a frame" — the frame as a *self-issued covering claim* over in-context choices.
- **A10** coherence-maintenance; challenges as verdict-moments; break-re-pricing.
- **A11** the coffee-shop decomposition (selector-premise vs entitlement-extension).
- **A12** the two dressers; `60` §2 context-fit; the user's own Hope-dependence line.
- **A13** "it's never alone" — the multiplicative architecture.
- **A14** the checklist-vs-frame distinction (consult-time vs runtime).
- **A15** bounds: no doc-demotion (compilation, not replacement); the entitlement edge decomposed without moralizing; "correct" scoped to frame-consistent; submit ≠ Supplication.

## Boundary Construction

In scope: the unification test; the ontology with assumability earned; the composition and compile-relation; the lifecycle; both examples; the successor's interface. Out of scope (hard): the best-frame adjudication (the user's explicit deferral); frame-control doctrine as ground; empirics; endorsing the entitlement edge; demoting the principle-docs.

## Perspective Checking

- **The wearer's perspective:** the frame is *relief* — the deliberation-cost drop is real and is the whole point ("we are not computers"). And the trap is symmetrical: a badly-chosen frame runs bad conduct at the same zero cost — automation cuts both ways, which is exactly why the best-frame question matters and is deferred, not dissolved.
- **The receiver's perspective:** frames are read *fast* — premise-posture lands in the g-channel's seconds-bandwidth; others meet your frame before your content. The frame is as much a communication as a control system.
- **The theory's perspective:** `03` audit — the frame must enter as a **named composition of committed objects** plus vocabulary (and the compile-relation); zero new machinery.
- **The adversary's perspective:** the field's "frame control" doctrine (impose yours, break theirs) must be neither imported nor ignored: frame-collisions are real and *already mechanized* — a frame carries a claim, and colliding frames are **claim-collisions** (the confrontation/ego machinery). The account names the phenomenon natively and prices hostile imposition with the existing extraction/consent tests; the dominance doctrine stays outside.

## Ambiguity Collapse

- `[frame-term-collision]` → **unification holds** (SV2): all four senses are stance-bundles differing by *object*; the Expressed Frame is the worn self-frame's display-face, not a sibling.
- `[definition-kind]` → all three delivered by one structure: the composition (ontology), from committed objects (mapping), lifecycle-shaped (function).
- `[scope-of-the-frame]` → **layered** (SV4): standing frames (bearing-level) beneath context-assumed frames; the claim-scope machinery carries the layers.
- `[assumability]` → **state-manufacture** (SV3, earned): premise-holding alone is the performed mode; the user's "not just information" is the co-generation mechanism speaking.

## Sense Versions

**SV1 — the import reading (unstable).** "Frame = frame-control" — the dominance doctrine (whose frame wins). Rejected as ground: excluded literature, reduces to display-management (the caught path), and has no formation story. Its one true fragment — frames *do* collide — re-enters lawfully in SV6 as claim-collision at the frame layer.

**SV2 — the unification test (the collision sorted).** Run each committed sense against the stance-bundle form (label + expected conduct + claim-scope):

- **Context-frame** ("this is a professional meeting"; theater's consented frame; the workplace's null): label ✓ conduct ✓ scope ✓ — object = *the situation*.
- **Pair-frame** ("what we are"): the definition's home — object = *the pair*.
- **The worn self-frame** (the user's sense: "I am the most important person in this coffee shop"): label (the selector/VIP) ✓ expected conduct (the walk, the ask) ✓ claim-scope (a *context-scoped* self-claim) ✓ — object = *self-in-context*.
- **The Expressed Frame** ("I'm the selector" vs "please like me"): tested as a possible fourth object — and it fails *as an object* while succeeding *as an edge*: it has no content independent of the self-frame; it **is** the self-frame as read from moves, with premise-posture (A5) as its per-move signature.

**Verdict: one family.** *A frame is a stance-bundle held at an object* — situation, pair, or self-in-context — and the "expressed" layer is the self-frame's readable face. The senses also **nest**: the coffee-shop self-frame presupposes a context-frame ("this is a place where I am served"); worn frames sit inside situation-frames, which is why the same self-frame plays differently across rooms.

**SV3 — the ontology, with assumability earned.** Test the candidates against the user's own criterion (assumable; "not just information"; generates action without deliberation):

- **Compact knowledge** fails — knowing the principles is the user's founding complaint: knowledge needs runtime interpretation, which *is* the computer-mode.
- **Compressed policy** ("always be selective") fails at the margin for the same reason: rules need per-situation application-judgment.
- **The premise + state compound** passes: assume the premise ("I am the selector here") → **manufacture the state it implies** (A4 — the actor's committed craft) → and the *state* does the rest, because **real states co-generate their conduct for free** (A3): the walk, the tempo, the unhurried order, the comfortable exit arrive without deliberation. The frame doesn't *summarize* the principles — it **installs a generator that produces the conduct the principles would have prescribed.** The compression is *generative, not informational* — which is precisely what "it's not just information" was reaching for.
- The premise-*only* reading (belief held coldly, state lagging) is the **performed-wearing** mode: the wobble leaks, the barista reads it, the arc prices it (A6).

**The ontology:** *a frame = a premise about self-in-context (a context-scoped stance-bundle) + the manufactured state that premise implies, worn as one unit.* Aspect-dual — belief-side (the premise) and state-side (the manufacture); **assumability = the ability to manufacture the implied state on demand**, which is trainable (the display professions' entire craft; the presence-path).

**SV4 — the lifecycle (the definition's dynamic half; every machinery-piece placed).**

1. **Assume:** choose the premise; manufacture its state. Cost = manufacture-effort, falling with practice (the presence-path).
2. **Wear:** the state co-generates conduct at zero deliberation-cost; **premise-posture** is what others read, in seconds; and every worn hour **grains both ledgers** (A8) — wearing is claim-formation in progress. "Submitting" to the frame (A9) is self-governance: the frame is a self-issued covering claim over in-context choices (the outfit chosen *by the frame*, not re-litigated by comfort) — submission to one's own frame is the opposite of Supplication (A15).
3. **Maintain:** the coherence machinery runs on the worn frame ("try this frame to be not broken"). **Challenges are verdict-moments** — the barista's pushback, the peer's test — and a frame held calmly is the claim-read's "defended, not qualified." **Three break-modes, one machinery:** self-contradiction (your own conduct violates the bundle) · challenge-loss (dropped when tested) · load-drop (fatigue collapses the manufactured state — the performed-wearing arc's fast lane). A break **re-prices retroactively**: the conduct the frame generated gets re-read as staging.
4. **Resolve:** frames are *droppable* (the context ends — legitimate, frame-consented exits exist), *breakable* (priced), or **wear-in**: repeated assumption lowers manufacture-cost until the state is standing — **the frame graduates into bearing** (`60` §8's own endpoint) / a standing self-claim (`21` §8). *Bearing is a frame worn until it stopped being worn and started being had.* The frame is the presence-path's unit.

**Layering (the scope answer):** standing frames (bearing; the formed self-claim) underneath; context-assumed frames on top (the coffee-shop VIP, for this visit); in-context choices are **frame-expressions** (the sharp-dresser's frame covers the comfort-question so it never re-opens).

**SV5 — the examples, and the relation to BDA and to us.**

- **The coffee-shop frame, decomposed:** the premise is a context-scoped selector/VIP self-claim; the manufactured composure co-generates exactly the SP catalog (unhurried walk, selecting orders, zero approval-seeking) → the gate reads high; the barista's service and deference feed back as grants, confirming the premise in both ledgers — *that* is why "it works," and its working core is the **Expressed Frame's committed selector-pole**. The **entitlement extension** ("he *has* to take care of me") is separable and adds nothing to the effect: the selector-premise generates the composure; the entitlement generates *demands* — extraction-shaped conduct that carries costs its wearer doesn't see (the barista pays them; and third parties read extraction fast — the date watching how you treat the barista is running the gate on you). Explained, decomposed, not moralized (A15).
- **The two dressers:** both frames are coherent stance-bundles. The sharp-frame converts *deliberate discomfort into a paid cost* — a backed display of respect-for-the-room that returns as in-context power ("you know that you dress up well"). The comfort-frame is an **independence display** — EW-adjacent, "I don't submit to dress codes" — valued exactly where autonomy reads as strength. "Neither is wrong" lands on committed machinery: **context-fit** (`60` §2) × **what each is hoping from that meeting** (the user's own Hope-dependence line) — which is the successor's exact territory. And "it's never alone" is the multiplicative architecture speaking (A13): frames modulate; the variables still carry.
- **Relation to BDA — the compile-relation:** the frame is **BDA's runtime object**. Principles are *training-time* (study, audit, practice — where frames get designed and their states get built); the compressed cards are *consult-time* (checklists); the frame is *runtime* (assume and wear). **Compilation, never replacement** (A15): the docs are the source the frame compiles from, and a frame not backed by the training is just the performed mode.
- **Relation to us:** the user's program already runs this architecture — bearing, the card, the one move, state-cultivation. The frame-concept **names the program's runtime layer**, and the user's field-observation ("people submit to a frame… it leads them to correct actions") re-derives the calc program's posture-answer from the outside.

**SV6 — the assembled definition and the successor's interface.**

> **A frame is a stance-bundle (label + expected conduct + claim-scope) held at an object — a situation, a pair, or oneself-in-context. The worn frame — the user's sense — is the self-directed member: a context-scoped premise about who-I-am-here plus the manufactured state that premise implies, worn as one unit: installed by state-manufacture (assumable, not memorizable), generating conduct by co-generation (zero deliberation — the runtime answer to "we are not computers"), read by others through premise-posture in seconds, maintained by the coherence machinery against challenges (which arrive as verdict-moments), graining the self-ledger while worn — and, worn long enough, graduating into bearing: the standing self-claim. It is BDA's runtime object: what the principles compile into.**

With the scoping the LOW-flag demanded: **a frame guarantees frame-consistent conduct at low cost — never attachment.** Whether the generated conduct attaches is the frame's *content-quality*, and that is exactly the deferred best-frame question. **The successor's interface** (what the best-frame inquiry will optimize): **content** (which premise — selector, craftsman, host, mission-carrier…) · **scope** (context-assumed vs standing; the claim's width) · **backing-requirement** (which premises the wearer's record and state-craft can actually manufacture — a frame too far from the record forces performed wearing) · **context-fit** (`60` §2's table) · **the extraction-test** (does the bundle's conduct generate or extract — the entitled edge's lesson). Frame-collisions (two frames claiming the same room) are claim-collisions at the frame layer — named natively, the dominance doctrine left outside.

## Saturation & Telemetry

Further passes add no anchors; SV6 delivers every Goal item inside every bound (best-frame untouched beyond its interface; no external ground; the docs un-demoted via the compile-relation; the entitlement edge decomposed with the barista's side noted and no lecture; submit≠Supplication stated). Failure modes checked: status-quo bias (the "compact knowledge" reading — the statement's own candidate — was tested and demoted, not deferred to), premature stabilization (the unification and assumability both *earned* via tests), anchor dominance (no single machinery-piece owns the frame — premise, state, coherence, and formation share it), perspective blindness (wearer/receiver/theory/adversary run; the automation-trap named), clean-resolution trap (the entitled frame's working-vs-extraction split kept as a real tension; the LOW-flag's scoping carried), self-reference blindness (the account notices the user re-derived the corpus's own posture-answer from the field — a confirmation, recorded as such). None fired.

**Hand to Decomposition: SV6.**
