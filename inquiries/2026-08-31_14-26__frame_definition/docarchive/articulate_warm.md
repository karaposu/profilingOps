# Articulate-Warm — frame_definition

## User Input

/Users/ns/Desktop/projects/profilingOps/inquiries/2026-08-31_14-26__frame_definition/_branch.md + /Users/ns/Desktop/projects/profilingOps/inquiries/2026-08-31_14-26__frame_definition/surfacing.md + /Users/ns/Desktop/projects/profilingOps/inquiries/2026-08-31_14-26__frame_definition/articulate_simple.md

## Item 1 — `frame-definition`

### Re-anchor: MQ2 re-fired against the surfaced material (anchor COMMITTED)

- **verdict (committed):** the account builds from the surfaced structure — the **unification candidate** (all four corpus frame-senses are *stance-bundles differing by object*: context / pair / self-in-context / the self-frame's expressed edge), the user's sense as **the worn self-frame**: an assumed, context-scoped self-claim plus its expected-conduct bundle, **installed by state-manufacture** (`60` §5), **generating conduct by co-generation** (the state produces words/timing/posture for free — the mechanism under "instead of thinking actively"), **maintained by coherence** ("not broken"), **read by others as a claim** (challenges are verdict-moments; premise-posture is its per-move signature), and **graining the self-ledger while worn** (the presence-path's unit — worn long enough, the frame becomes bearing / a standing claim). The frame as **BDA's runtime object — what the principles compile into**; the coffee-shop decomposition (working part = the selector-premise; the entitlement extension separable); the two dressers at `60` §2's context-fit + Hope-dependence; the checklist/frame distinction (training-time vs runtime objects).
- **kinds (committed):** the **frame** resolves toward the *composition* (premise/self-claim + stance-bundle + manufactured state) — the "compact knowledge" option is demoted by the surface: knowledge doesn't generate conduct at zero deliberation-cost, states do; the user's "not just information" is the mechanism speaking. **Wearing** = state-manufacture, with performed-wearing as the priced alternative. **Broken** = coherence-contradiction (self-generated, or a challenge won against it, or dropped under load — three break-modes, one machinery).
- **stance (committed):** meaning-layer; the successor's interface exposed (content · scope · backing-requirement · context-fit across the frame's lifecycle), never run.

### Anchor-movement check → re-surface decision

The surface satisfied the cold context-need and organized it; nothing relocated. **FIXPOINT at round 0; no re-surface.**

### Trigger-gated re-runs

- **MQ1 — MOVED (two resolved, two weighted), re-run.** `[frame-term-collision]` → tilted to **unification** (one family, stance-bundles by object) — Sensemaking must *test* it, especially whether the Expressed Frame truly sits as the self-frame's displayed edge rather than a sibling. `[assumability]` → resolved-by-surface toward **state-manufacture** — S must earn it against the premise-holding alternative. `[definition-kind]` → all three delivered by the composition (ontology + mapping + function). `[scope]` → tilted **layered**: standing self-frames (bearing-level) and context-assumed frames (the coffee-shop) — the claim-scope machinery already handles layering.
- **MQ4 — MOVED, re-run.** Two additions: **(i) no demotion of the principle-docs** — the runtime-object framing must state the relationship as *compilation* (the docs are the source the frame compiles from; training-time artifacts remain load-bearing), never replacement; **(ii) the entitlement edge handled by decomposition** — the coffee-shop frame's working part (the selector-premise) separated from its extraction-shaped extension (the demands on the barista), with the barista's side noted honestly and without moralizing — the branch's balance held.
- **MQ3 — unmoved.** All six endpoints live.
- **MultiDepth-WHY — unmoved.**
- **MQA — re-run (≥2 re-ran).** The moved axes join into the account's committed shape: the definition should be **lifecycle-shaped** — *assume → wear (co-generate + grain) → maintain (coherence; challenges as verdict-moments) → drop, break, or wear-in (the frame becoming bearing / a standing claim)* — which simultaneously delivers the ontology, the mapping, the function, AND the successor's interface (the best-frame question will optimize content/scope/backing/context-fit over exactly this lifecycle). Sensemaking must test the lifecycle-shape rather than inherit it.

### Conflict-detection (warm-only)

1. **Explicit no-conflict — confirmation.** "We cannot operate such way. We are not computers" is the calculation program's *founding constraint*, already committed, with the posture-answer as its committed resolution — the user's frame-move is the corpus's own move, arriving from the field.
2. **`content-conflict` — LOW severity — "leads them to do correct actions."** The corpus cannot underwrite "correct" as attachment-effective for *any* frame — a frame can generate perfectly frame-consistent conduct that detaches (the entitled frame's extraction edge). Resolves by scoping: a frame guarantees *frame-consistent* action at low cost; whether those actions serve attachment is the frame's **content-quality**, which is exactly the deferred best-frame question. The finding must state this scoping plainly. No question needed.
3. **Explicit no-conflict — "submit to a frame" vs Self-Positioning.** Submission to one's *own* frame is self-governance (a self-issued covering claim over in-context choices), not Supplication — worth one explicit line, since "submit" could misread against the gate.

### Re-fired Rephrase (anchor settled; warm-sharpened variant-set)

1. "Sort the collision by unification-test: are all four senses stance-bundles differing by object (context / pair / self-in-context / expressed edge) — and does the Expressed Frame sit as the worn self-frame's displayed edge?"
2. "Commit the lifecycle-shaped ontology: the frame as an assumed, context-scoped self-claim + stance-bundle, installed by state-manufacture, co-generating conduct, coherence-maintained, ledger-graining — with 'compact knowledge' demoted by the co-generation mechanism ('not just information' vindicated mechanically)."
3. "Name the composition and the compile-relation: which committed objects assemble (self-claim at chosen scope; stance-bundle; manufactured state; premise-posture as signature), and how the principle-docs relate as compile-source (training-time) to the frame as runtime object — replacement excluded."
4. "Adjudicate assumability and the two wearing-modes: assume = manufacture (the presence-path's unit; wear-in → bearing); performed-wearing as the priced alternative (leakage; the arc); the three break-modes under one coherence machinery."
5. "Explain the examples and expose the successor's interface: the coffee-shop frame decomposed (selector-premise working; entitlement-extension separable, barista-side noted); the two dressers at context-fit + Hope-dependence ('neither is wrong'); the interface — content · scope · backing-requirement · context-fit over the lifecycle — for the deferred best-frame inquiry."

### Carried through unchanged

Itemize (1 item) · Deconstruct tuple (MQ4's two additions folded into bounds) · MultiDepth literal-statement · the noise-register.

---

## Self-Assessment

Warm failure modes checked: false-positive-conflict (the "correct actions" tension is textually real against the multiplicative architecture), crying-wolf (one LOW + two no-conflicts, one a confirmation), adjudicate-instead-of-identify (the unification and lifecycle handed to S as tests; assumability's resolution to be earned), ignore-the-trigger (MQ3/WHY un-rerun with reasons). Inherited modes: 2-shape held.

**Verdict: HIGH-PROCEED** · flags: one `content-conflict` (LOW — "correct actions" scoped to frame-consistency; content-quality deferred to the successor; scoping owed plainly in the finding).
