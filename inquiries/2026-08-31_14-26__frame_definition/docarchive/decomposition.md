# Decomposition — frame_definition

## User Input

/Users/ns/Desktop/projects/profilingOps/inquiries/2026-08-31_14-26__frame_definition/_branch.md + /Users/ns/Desktop/projects/profilingOps/inquiries/2026-08-31_14-26__frame_definition/sensemaking.md

## Step 1 — Coupling Topology

- **Tight-central:** the ontology (P2 — the premise+state compound with its two wearing-modes) — the lifecycle, the examples, and the interface all consume it.
- **Tight:** the unification (P1) ↔ the ontology (the stance-bundle form is the compound's belief-side; the family gives the definition its genus).
- **Medium:** the lifecycle (P3) ↔ formation/coherence machinery (transfers consumed, separable as the dynamic half).
- **Medium:** the examples (P5) ↔ P1–P3 (worked applications).
- **Thin:** the compile-relation (P4); the successor interface (P6); homing (P7).
- **Load-bearing joints (flag for I/C):** (i) **the Expressed Frame's placement** (edge-not-sibling — the unification's most contestable move); (ii) **the generative-compression claim's completeness** — co-generation explains produced conduct, but frames also *pre-exclude options* (the sharp-dresser's comfort-question never re-opens): is choice-exclusion a co-generation effect or a separate premise-side function?; (iii) **the bearing-graduation claim** ("bearing is a frame worn until it stopped being worn") — elegant, possibly overreaching `60` §8's text.
- **Cross-cutting:** the Synthesis-Trigger re-test (6 priors) — owner: the finding format.

## Step 2–3 — Boundaries (validated bottom-up)

- **P1 — the unification.** Stance-bundles by object (situation / pair / self-in-context); the Expressed Frame as the self-frame's display-face; nesting (self-frames presuppose situation-frames).
- **P2 — the ontology.** The premise + manufactured-state compound; assumability = state-manufacture (earned against knowledge/policy); the two wearing-modes (manufactured vs performed); "not just information" mechanized.
- **P3 — the lifecycle + layering.** Assume → wear (co-generate + grain; premise-posture; submit-as-self-cover) → maintain (three break-modes; challenges as verdict-moments; break-re-pricing) → drop / break / wear-in (bearing-graduation); standing frames under context-assumed ones.
- **P4 — the compile-relation.** Training-time (principles) → consult-time (cards) → runtime (the frame); compilation never replacement; unbacked frames = the performed mode.
- **P5 — the examples.** The coffee-shop decomposition (selector-pole working; entitlement-extension separable; the barista's side); the two dressers (paid-cost display vs independence display; context-fit × Hope).
- **P6 — the interface + guards.** The five successor-parameters (content / scope / backing-requirement / context-fit / extraction-test); the frame-consistency-not-attachment scoping; frame-collision named as claim-collision; the automation-trap (bad frames run bad conduct at zero cost).
- **P7 — the homing map.** Vocabulary + the definition into the canonical layer; the successor registered at `80`; rows; the history entry.

## Step 4 — Question Tree with Verification Criteria

- **P1: Are the four senses one family?** ✓ each tested against the stance-bundle form; ✓ the Expressed Frame's edge-placement argued (no independent content; premise-posture as its signature); ✓ nesting stated; ✓ no sense's committed text requires change.
- **P2: What kind of thing is a frame?** ✓ the candidates each tested against the user's own criterion (assumable; no-deliberation generation); ✓ knowledge and policy demoted *by test*, not assertion; ✓ the compound stated aspect-dually; ✓ the performed mode priced; ✓ zero new machinery (named composition).
- **P3: How does a frame live?** ✓ four lifecycle stages each running on committed machinery; ✓ three break-modes under one coherence engine; ✓ wear-in → bearing stated with its ground (`60` §8's endpoint + the presence-path) — or honestly weakened if critique demands; ✓ layering via claim-scope.
- **P4: How do frames relate to the principles?** ✓ the three-time-scale relation (training / consult / runtime); ✓ replacement explicitly excluded; ✓ the unbacked-frame case routed to the performed mode.
- **P5: Do the examples resolve?** ✓ coffee-shop decomposed with the working-part identified and the extraction edge separable (barista-side noted, no lecture); ✓ the dressers land on context-fit × Hope with both frames honored; ✓ "it's never alone" mapped to the multiplicative architecture.
- **P6: Is the successor served?** ✓ five parameters named and each grounded in committed machinery; ✓ the scoping stated (frame-consistency ≠ attachment); ✓ collisions named natively; ✓ the automation-trap stated.
- **P7: Where does it land?** ✓ owning files named; ✓ the term enters as vocabulary + composition; ✓ the successor registered.

## Step 5 — Interface Map

- P1 → P2: the stance-bundle is the compound's belief-side; P2 adds the state-side.
- P2 → P3: the two wearing-modes are the lifecycle's rails (manufactured wear vs the performed lane's fast break).
- P2 + P3 → P5: the examples consume the compound and the lifecycle vocabulary only.
- P3 → P6: the lifecycle's stages are the interface's optimization surface.
- P2's zero-machinery property → P7's `03` standing (explicit, per precedent).
- P6 exports the five parameters to the successor's registration (P7 carries them into `80`).
- Cross-cutting: the re-test layer audits P1 against the backing/window-close frame-texts, P2 against `60` §5 + the co-generation commitment, P3 against the formation finding + coherence, P4 against the calc program, P5 against `60` §2.

## Step 6 — Dependency Order

**P1 → P2 → P3 → {P4 ∥ P5} → P6 → P7.** P4 and P5 are independent given P1–P3; P6 consumes all; P7 last.

## Step 7 — Self-Evaluation

- **Completeness:** Goal items map (collision→P1; ontology+assumability→P2; lifecycle/scope→P3; BDA-relation→P4; examples→P5; successor-interface→P6; homes→P7). No orphan.
- **Hidden coupling:** the performed-wearing mode serves P2, P3, and P4 — one mode, three consumers, carried as P2's export. The claim-scope machinery serves P1 (nesting) and P3 (layering) — one vocabulary. None hidden on re-walk.
- **Balance:** P2 and P3 heaviest; each one coherent unit. Accepted.
- **Verification coverage:** per-piece criteria; the re-test layer owned; the three flagged joints assigned to I/C.
- **Dependency soundness:** linear with one parallel pair; no cycles.
- **Over-decomposition check:** nesting stays in P1; layering in P3 (different facts, same vocabulary — cross-referenced, not merged); the automation-trap stays in P6 (a guard, not a piece).
- **Stopping criterion:** question-shaped, verifiable, answerable from SV6. Stop.

**Hand to Innovation: seven pieces; the flagged joints — the Expressed Frame's placement, the choice-exclusion question (co-generation's completeness), and the bearing-graduation claim.**
