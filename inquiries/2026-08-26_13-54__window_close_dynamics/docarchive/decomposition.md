# Decomposition — window_close_dynamics

## User Input

/Users/ns/Desktop/projects/profilingOps/inquiries/2026-08-26_13-54__window_close_dynamics/_branch.md + /Users/ns/Desktop/projects/profilingOps/inquiries/2026-08-26_13-54__window_close_dynamics/sensemaking.md

## Step 1 — Coupling Topology

- **Tight:** fit-selection ↔ deceleration ↔ conduct-recording — the hardening triple (one mechanism, three legs; splitting them orphans the three-phase shelf-life shape).
- **Tight:** the hardening ↔ the liveness rider (complementary competition + the rider; SV3's three-stage arc is their joint product).
- **Tight:** the reopening split ↔ the liveness arc (re-raise/re-answer derives directly from "no longer a question" + graded recording).
- **Medium:** the attractor-field ↔ the mechanism (the field supplies the candidates and nulls the mechanism selects over — input interface, separable).
- **Medium:** rate-factors ↔ multiplication (both driven by cycles and contact-surface; one dynamics piece).
- **Thin:** the FR2 one-clock statement (an export); the dark dual (a named boundary object); the worked case (a consumer of everything).
- **Load-bearing dependency:** the **zero-import property** again (all mechanism parts reduce to committed machinery; imported vocabulary flagged as presentation) → the account's FR2-native standing.
- **Cross-cutting:** the Synthesis-Trigger re-test (6 priors) — owned by the finding format.

## Step 2–3 — Boundaries (validated bottom-up)

- **P1 — the hardening mechanism.** Fit-selection (non-asking as label-predicted conduct) + deceleration + conduct-recording (micro-declarations, both sides) + the silent threshold.
- **P2 — the liveness rider.** The Hope adjudication (one competitive process + liveness-decay); the three-stage arc (open → closed → no longer a question).
- **P3 — the attractor-field.** Context-supplied nulls; enforcement-strength; substance-under-surface (forms); the three ledgers and audience-first settling.
- **P4 — the dynamics.** The six rate-factors; multiplication as co-growth of capacity and contact-surface; the uncontrolled-close recording hazard; the FR2 one-clock statement.
- **P5 — the reopening adjudication.** Re-raise (frame-discrepant / new-context evidence) then re-answer (graded recording); the cost-structure contrast with declared no's; the inherited bounds.
- **P6 — the boundary objects + the worked case.** The friend-zone natively mechanized; engineered liveness named (breadcrumbing = intermittent reinforcement's frame-level face); settling de-pathologized.
- **P7 — the homing map.** T11 discharged; `41` §8 and `60` §11 annotations; `02` rows; `80` updates; `90` entry.

## Step 4 — Question Tree with Verification Criteria

- **P1: How does a frame harden without any verdict-event?** ✓ three legs each from committed machinery (A2/A3/A4); ✓ non-asking shown as *positive* confirmation (label-predicted), not neutral absence; ✓ the threshold silent (explains discovered-not-announced); ✓ zero-import audit passes with vocabulary flags.
- **P2: Is Hope-expiry a second process?** ✓ adjudicated (complementary competition + genuine liveness rider); ✓ the three-stage arc stated; ✓ the settled end-state = "the question left the active set," not "she decided no."
- **P3: What is there to settle into, and whose ledger?** ✓ context-nulls with enforcement-strength; ✓ same-conduct-different-label consequence explicit (app vs workplace vs friend-group); ✓ three ledgers; ✓ audience-first + feedback derived; ✓ forms-inventory undisturbed (one line).
- **P4: What sets the pace, and why does forcing multiply?** ✓ six factors, shape-level only; ✓ multiplication derived as co-growth (no physics residue); ✓ the public-forcing recording hazard stated; ✓ the FR2 statement (one observation-ledger, several state-variables) explicit; ✓ parameter-identity with detection-lag named.
- **P5: Can a settled frame re-open, at what cost?** ✓ two ordered problems; ✓ new-context evidence as the clean re-raise route; ✓ cost graded by recording (never "cheap because unspoken" — the warm correction); ✓ the contrast with question-change stated; ✓ revival-attempts-are-asks (60 §11 governs; no playbook).
- **P6: Does the account discharge its exemplar and guard its misuse?** ✓ friend-zone mechanized natively, folk term dissolved; ✓ engineered liveness named and tied to `50`'s adjacency list; ✓ settling described as functional cognition.
- **P7: Where does it land?** ✓ T11 → discharged-analytically status; ✓ `41` §8 gains the developed dynamics; ✓ `60` §11 gains the shelf-life note; ✓ `02` rows; ✓ `90` entry; ✓ stamps.

## Step 5 — Interface Map

- P3 → P1: the field supplies candidates and nulls; P1 selects over them (P1 never opens P3's interior).
- P1 + P2 → P5: the arc's end-state and the recording-grade are the reopening problems' premises.
- P1 → P4: cycles and same-shapedness drive both factors and deceleration; P4 exports the co-growth claim back to `60` §11's "near peak, ask deliberately" line as its derivation.
- P1–P5 → P6: the worked case consumes the full vocabulary; the dark dual consumes P2's liveness + P4's perturbation factor.
- P1's zero-import property → P4's FR2 standing (explicit, as in the parent inquiry).
- All → P7.
- Cross-cutting: the re-test layer audits P1 against `20`/iteration-3.4/M4, P2 against Hope's machinery, P5 against the parent grading, P6 against `50`'s adjacency list.

## Step 6 — Dependency Order

**P1 → {P2 ∥ P3} → P4 → P5 → P6 → P7.** P2 and P3 are independent after P1; P4 needs P1's cycle-machinery; P5 needs P1+P2; P6 consumes all; P7 last.

## Step 7 — Self-Evaluation

- **Completeness:** Goal items map (mechanism→P1; liveness→P2; attractors→P3; rates+multiplication+FR2→P4; reopening→P5; worked case + guards→P6; homing→P7). No orphan.
- **Hidden coupling:** the liveness rider serves both P2 and P5 — carried as an exported arc (P2's product, P5's premise), stated once. The dark dual sits in P6 but consumes P2/P4 exports — interfaces named. None hidden on re-walk.
- **Balance:** P1 and P4 heaviest; each one coherent unit (a triple; a dynamics set). Accepted.
- **Verification coverage:** all pieces carry checkable criteria; the re-test layer has its owner.
- **Dependency soundness:** linear with one parallel pair; the P1→P4 export-back (co-growth grounding the doctrine line) is one-directional content, no cycle.
- **Over-decomposition check:** the silent threshold stays in P1 (a property, not a piece); the FR2 statement stays in P4 (an export, not a piece); the friend-zone and the dark dual share P6 (both are boundary-discharge work).
- **Stopping criterion:** question-shaped, verifiable, answerable from SV6. Stop.

**Hand to Innovation: seven pieces; the most exposed joints — P2's rider (is liveness-decay real machinery or a redescription?) and P5's two-problem split (does it survive adversarial cases?).**
