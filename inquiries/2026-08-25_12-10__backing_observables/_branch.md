# Branch: backing_observables

## Source Input
[The user's raw request, preserved verbatim. Also lives in articulate_simple.md's `## User Input` section; both copies are authoritative for transcription audit.]

```text
6 · the backing-observables inquiry — Goal: the standing gate opened · INVESTIGATE-FRONTIER (teleological) Move: open the corpus's G1 as its own inquiry — now with a third consumer (the deployed-exit's discrimination from leaving-to-be-chased inherits it, alongside the calc program's self-check and the boundary zone). Lands: backed-vs-performed observable; three consumers unblocked. WHY: this inquiry added another demand-line to the corpus's most-demanded open question; the goal's account is honest about inheriting it. Priority: MED (loud elsewhere; this inquiry only registers demand) · Confidence: HIGH · Essentiality: supporting
```

## Articulation Reference

- **File:** `inquiries/2026-08-25_12-10__backing_observables/articulate_simple.md`
- **Itemize count:** 1
- **Per-item identifiers:** `backing-observables`
- **Verdict:** HIGH-PROCEED
- **Flagged conditions:** none

## Question

**Literal statement (MultiDepth):** "Open the corpus's standing gate — what observably separates a backed display from a performed one — as its own inquiry; land the backed-vs-performed observable(s); unblock the consumers (the narcissist layering's genuine/mimicked distinction, the calculation program's self-check, the rehearsed-forms boundary zone, and the deployed-exit discrimination the insistence inquiry just added)."

**What kinds of ask the statement carries (MQ1, preserved as ambiguities):**
- `[observable-SET / single-criterion / graded-instrument]` — a list, one criterion, or a probabilistic instrument with grades;
- `[second-person / first-person / both]` — reading others, self-checking one's own backing, or both forms;
- `[possibility-verdict-first]` — the implicit prior question: is backing observable at all? (the corpus's own admission: "currently unfalsifiable in practice");
- `[boundary-adjudication-included?]` — is the rehearsed-forms zone adjudicated here or merely served.

**Plausible action-endpoints (MQ3, preserved):** `[derive-why-observable]` · `[name-the-observables]` · `[adjudicate-the-boundary-zone]` · `[unblock-the-consumers]` · `[upgrade-the-falsifiability-status]`.

## Goal

**Deconstruct tuple:** (deliverable: the backing-observables account — the whether/why verdict, the named observable-set with confidence grades and reading-conditions, the boundary-zone adjudication, per-consumer unblocking statements; kinds: corpus-grounded conceptual analysis through the full pipeline into a finding, then homed per the maintenance contract; bounds: the backed/performed definition unchanged, conduct/artifact-level observables only, structural-only status inherited, the registered consumers served).

**WHY-axis motivation-ambiguities (preserved):** `[gate-clearing]` (the most-demanded open item; display-training and several findings' flags wait) / `[integrity]` (the narcissist account, the priced display-path, and the decay arc all rest on a currently-untestable joint) / `[application]` (the first-person self-check for the user's conduct program) / `[boundary-adjudication]` (the rehearsed-opener question blocks preparation guidance).

**Context downstream consumers need (MQ2, preserved):**
- **verdict:** the backed/performed definition (`BDA/21` §5 — defined by contrast only); the costly-signal ground ("fake Self-Positioning is detectable because the cost isn't actually paid" — iteration-1); the mimicry/decay arc + Coherence complement; the **already-committed detection moments** — the calibrating performer, performed anger's "he's putting it on" report, EC's Affect-Content Congruence ("not forced or performed"), Coherence's Alignment aspect + longitudinal catalog, the anxious-distracted edge, the sequence-shape-as-display, Signal Specificity/template-match; the Stanislavski state-manufacture note (`60` §5); the "currently unfalsifiable" admission's source; the G1 consumer-register (`BDA/80`).
- **kinds:** "observable" = `[per-instance vs in-expectation; unaided vs instrumented; real-time vs retrospective]`; "a display" = `[a move / a scene / a sequence / cross-context conduct / off-stage artifacts]`.
- **stance:** meaning-layer fundamentals (what backing IS such that it can or cannot be observed) with consumer-facing outputs.
- **Context-shift note (for Warm):** the route text says three consumers; the register has since recorded a **fourth** (the deployed-exit discrimination).

**What would explicitly fail (MQ4 NOT-list, preserved):** redefining backed/performed (the definition stands — observables FOR it); claiming empirical validation (specify and grade, never measure); any interior-access "observable" (conduct/artifact/context level only).

**MQA joint axes (reconciled):** (1) the **possibility+mechanism axis** — whether and why backing is observable comes first; every observable presupposes it; (2) the **instrument-shape axis** — the answer must declare its own confidence-shape (per-instance vs in-expectation; unaided vs instrumented) rather than leave "observable" ambiguous.

## Considered Articulations

**Item `backing-observables`:**
1. "Determine whether and why backing is observable at all — the mechanism by which a backed display differs detectably from a performed one — and render the updated falsifiability-status verdict."
2. "Name the observable-set: concrete conduct-level observables (in-the-moment, under-perturbation, cross-channel, over-time, off-stage) separating backed from performed displays, each with its confidence grade and reading-conditions."
3. "Adjudicate the rehearsed-forms boundary zone: where does preparation cross into performance — and does state-manufacture (the display professions' own method) count as backing?"
4. "Build the first-person instrument: the self-check form of the observables for the calculation program's 'is my cultivated bearing backed?'"
5. "Unblock the four registered consumers: state what the narcissist genuine/mimicked distinction, the program self-check, the boundary zone, and the deployed-exit discrimination each receive."

## Synthesis Trigger

This inquiry consumes prior outputs as inputs — the scattered detection-route commitments must roll up into one account. Each prior carries commitments the finding must re-test (CONCLUDE will require an `## Inherited Commitments Re-test` section):

- `inquiries/apt_belief_attributes/finding.md` — the admission this gate descends from: the genuine/faked (backed/performed) distinction is **"currently unfalsifiable in practice."** The inquiry's output must state what happens to this admission.
- `inquiries/narcissist_attachment_layering/finding.md` — the genuine/mimicked distinction as load-bearing; the calibrating-performer read ("approval-fishing even if technically competent"); monitoring-leakage as a detection moment.
- `inquiries/2026-08-25_10-32__insistence_versus_exit_willingness/finding.md` — the fourth consumer (deployed exit vs leaving-to-be-chased); sequence-shape-as-display; conviction-display (M5) as a display whose backing matters.
- `inquiries/2026-08-24_19-58__leadership_as_calculation/finding.md` — the self-check consumer ("is my cultivated bearing backed?"); the rehearsed-forms boundary zone; the Stanislavski state-manufacture line.
- `BASEFILES/.../iteration_1*` (via `BDA/21` §5) — the costly-signal ground: "fake Self-Positioning is detectable **because the cost isn't actually paid**."
- `BASEFILES/.../iteration_3*` (via `BDA/20`) — the mimicry **decay arc** (performances decay under sustained observation) and the Coherence complement (cross-context persona collapse).
- `BDA/20` — EC's **Affect-Content Congruence** signal ("not forced or performed") and Coherence's Alignment aspect + longitudinal catalog — the corpus's already-catalogued observables this inquiry must reconcile with rather than duplicate.
- `BDA/30` — performed anger's observer-report ("he's putting it on") — evidence that unaided detection **happens**, which any impossibility verdict must survive.

## Scope Check

Question covers goal. Deconstruct's bounds (definition unchanged; conduct-level; structural-only; consumers served) cover the Goal's demands; the MQ4 exclusions appear above: **no redefinition, no empirical claims, no interior access.**

**Specific-vs-pattern check:** the gate is stated generally (any display, any species) while the consumers name specific cases (mission-display, exit moves, rehearsed openers, the user's bearing). **Address the GENERAL pattern** — backing-observables for displays as such — with the four consumer cases as the mandatory worked applications; a general account that fails any registered consumer's case is incomplete.
