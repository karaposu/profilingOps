# Decomposition — backing_observables

## User Input

/Users/ns/Desktop/projects/profilingOps/inquiries/2026-08-25_12-10__backing_observables/_branch.md + /Users/ns/Desktop/projects/profilingOps/inquiries/2026-08-25_12-10__backing_observables/sensemaking.md

## Step 1 — Coupling Topology

The object is SV6 (the stable account) on its way to a finding + homed state. Coupling gradients observed:

- **Tight:** the root asymmetry ↔ the channel-list (each channel *is* one composition-cost surfacing; neither stands alone).
- **Tight:** channels ↔ their reading-conditions ↔ their false-positive profiles (a channel without its hazard is `31`-violating).
- **Medium:** the channel *partition* (1–3 vs 4–7) ↔ the occurrent/standing split (the split is read off the partition; the partition is motivated independently by reading-conditions).
- **Medium:** convergence rule ↔ adversarial limit (the limit is *why* grades exist; the rule is *how* they combine).
- **Thin:** frame-consent ↔ everything else (feeds only the boundary-zone adjudication).
- **Thin:** each consumer ↔ its channel-subset (narcissist ← full second-person set; calc program ← first-person inversion; boundary zone ← split + frame-consent; deployed exit ← channels 2/5/7).
- **Cross-cutting, not a piece:** the Synthesis-Trigger re-test duty (8 priors) spans every piece — a verification layer over the whole, owned by CONCLUDE's finding format.

## Step 2–3 — Boundaries (top-down, validated bottom-up)

Six pieces at the thin seams; bottom-up validation confirms no piece needs another's *interior* (only their exported interfaces):

- **P1 — the mechanism.** Root asymmetry: state-generated vs controller-generated output; the composition-cost list.
- **P2 — the instrument.** Seven channels × (reads-what · reading-condition · corpus home · false-positive profile · defeat-mode) + the convergence rule and grades.
- **P3 — the reading-split + frame.** Occurrent vs standing backing; frame-consent; the boundary-zone adjudication.
- **P4 — the status-verdict.** From "currently unfalsifiable in practice" to structurally-falsifiable-in-expectation; the adversarial limit; what stays unavailable.
- **P5 — the consumer applications.** Four worked readings.
- **P6 — the homing map.** Owning files per the maintenance contract; signals-not-elements enforcement.

## Step 4 — Question Tree with Verification Criteria

- **P1: Why would a backed display differ observably from a performed one at all?**
  - ✓ every surfaced seed (cost-trail, decay, alignment, specificity, sequence-shape) derives from the one mechanism; ✓ no seed left orphaned; ✓ no outcome term or interior access smuggled in.
- **P2: What exactly can be read — when, where, against what error?**
  - ✓ all seven channels carry the full five-field row; ✓ convergence rule explicit; ✓ no channel claims solo certainty; ✓ every channel names its corpus home or declares itself a new *signal* under an existing element.
- **P3: What does "actually there" read as under the Stanislavski limit, and where does preparation cross into performance?**
  - ✓ classifies the actor (occurrent-yes/standing-no), the calibrating performer (typically both-no), the rehearsed founder (both-yes) correctly; ✓ `21` §5's definition text needs no change; ✓ frame-consent covers consensual performance; ✓ the crossing-line is stated in one sentence (state absent, not form practiced).
- **P4: What is the distinction's falsifiability status now?**
  - ✓ the old admission quoted verbatim and addressed; ✓ the new status claims exactly what the structure supports (in-expectation, uncalibrated); ✓ the adversarial limit stated as the account's own boundary (detects cheap performance well, complete performance not at all — because complete performance is backing).
- **P5: What does each consumer receive?**
  - ✓ four consumers × (channel-subset + worked reading); ✓ the exit case actually discriminates deployed vs leaving-to-be-chased (monitoring-for-pursuit; off-stage completion; sequence hinging on being chased).
- **P6: Where does this land as state?**
  - ✓ each piece has an owning file; ✓ nothing enters `02` as a new element; ✓ `80` G1's ledger updated (opened → answered-structurally, consumers pointed at the finding).

## Step 5 — Interface Map

- P1 → P2: the composition-cost list **generates** the channel rows (each cost → the mark it surfaces as).
- P2 → P3: the reading-condition partition (in-moment vs continuity channels) is the split's **evidence**; P3 consumes the partition, not the rows' interiors.
- P2 → P4: the per-channel defeat-modes + false-positive profiles justify **grades**; P4 exports the limit-statement back as P2's grading rationale (declared two-way interface, kept explicit to avoid hidden coupling).
- P2 + P3 → P5: instrument + split applied per consumer; P5 consumes exported channel names and the split's verdict-vocabulary only.
- P1–P5 → P6: homing consumes the finished pieces' one-line forms.
- Cross-cutting: the re-test layer (Synthesis Trigger) audits P1 against iteration-1/-3, P2 against `20`'s catalogs, P3 against `60` §5 + `21` §5, P4 against the belief-attributes admission, P5 against the three consumer findings.

## Step 6 — Dependency Order

**P1 → P2 → {P3 ∥ P4} → P5 → P6.** P3 and P4 are independent after P2 (split needs the partition; verdict needs the grades) and can be developed in either order; P5 needs both; P6 last.

## Step 7 — Self-Evaluation

- **Completeness:** the Goal's four deliverables map onto pieces (whether/why → P1+P4; observable-set → P2; boundary adjudication → P3; consumer unblocking → P5) with homing (P6) closing the maintenance contract. No orphan deliverable.
- **Independence / hidden coupling:** the two flagged medium couplings (partition↔split; rule↔limit) are carried as explicit interfaces; hazards live in P2 and are *exported with* the channels so P5 can't read channels hazard-free. No hidden coupling found on re-walk.
- **Balance:** P2 is the heaviest piece (seven five-field rows) but is one coherent table at one grain; splitting it per-channel would shatter the convergence rule's home. Accepted.
- **Verification coverage:** every piece has checkable criteria (above); the cross-cutting re-test layer is assigned an owner (finding format) rather than floating.
- **Dependency soundness:** no cycles; the one two-way interface (P2↔P4) is content-partitioned (consequence in P2, statement in P4), so build order stays linear.
- **Over-decomposition check:** frame-consent stays inside P3 (thin but not load-bearing enough to stand alone); the first-person inversion stays inside P5's calc-program application (it is an *application form* of P2, not a separate instrument).
- **Stopping criterion:** pieces are question-shaped, verifiable, and each is answerable in one sitting from SV6. Stop.

**Hand to Innovation: the six pieces, with P1→P2 as the generative spine and P3's split as the account's most exposed (most attackable) joint.**
