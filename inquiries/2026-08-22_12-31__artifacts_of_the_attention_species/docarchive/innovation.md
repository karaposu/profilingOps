# Innovation — artifacts_of_the_attention_species

## User Input

```text
/Users/ns/Desktop/projects/profilingOps/inquiries/2026-08-22_12-31__artifacts_of_the_attention_species/_branch.md + sensemaking.md + decomposition.md
```

---

## Phase 1 — Seed

**The seed.** SV6's stabilized model plus the eight-piece plan: both horns fail on the statement's own criteria; the bearer is `object × observer`; the species is a proxy whose one recorded failure is the statement's own teacher; the portability premise is conditional on an undeclared choice; the verdict is *artifact*-sense-independent; `I2`'s class has no first member, so the answer is a test plus a sorted four-item sample (object ×2, level ×2, species ×0), with the census scheduled as the next run's Process work. Seed type: a **question** developed by sensemaking into a **collision** (both horns refused; the corpus's third term) plus a **gap** (no corpus term for *real but derived*; a class with no members).

### Methodology-Mode Consideration

- **(a) Inherited mode: Standard default.** The seed framing carries no mechanism weighting — SV6 commits a verdict and the decomposition commits a piece-list to elaborate; the text signals are "deliver the two-part answer," which is elaborate-the-committed-direction.
- **(b) Alternative mode: Contrarian-rethink (Framer-weighted).** The central commitments rest on a four-item sample (A6 at MEDIUM-to-HIGH) and the Risk perspective itself flagged the relocation-pattern hazard — grounds for a Framer-heavy challenge run.
- **(c) What follows under the alternative.** The candidate space would re-litigate SV6's verdict (attack `object × observer`, attack the proxy claim) rather than generate content for the finding. Since sensemaking's Phase 4 already ran the strongest counter-interpretation against every one of its eight ambiguities and the stabilization audit checked anchor-dominance by removal (three independent pillars), a Framer-weighted run would largely repeat adversarial work already performed upstream.
- **(d) Decision: default — run Standard default.** The verdict-level contrarian work is done; Innovation's marginal value is candidate generation for the finding's content plus the piece-level Inversions the meta-decision pieces require regardless of mode. Full 7-mechanism coverage.

**Production-task mode is operating** — the seed includes a piece-list (P1–P8). Per-piece classification and piece-level Inversions are in the dedicated section after the mechanism outputs.

---

## Phase 2 — Mechanism Outputs

### 1. Lens Shifting (Framer)

**LS-G (generic) — the finding as instrument, not verdict.** Current frame: the deliverable is an answer to the user's disjunction. Shifted frame: the deliverable is a **diagnostic habit** — one question, *which axis is this claim predicated on?* — that the corpus can run on every future per-species claim. Under the answer-frame the verdict is the product and the sorting is support; under the instrument-frame the sorting method is the product and the verdict is its first application. Conditions under which the instrument-frame wins: the corpus keeps producing per-species claims (it does — two titled spec sections, fifteen scenarios). Develops KI18 from a strategic note into deliverable-shape guidance: the finding should present the check as reusable, citing its two demonstrated catches (the Hope-amplification error; the Scenario 4 fork).

**LS-F (focused) — emptiness as the criteria biting.** Current frame: four items, none species-predicated → thin, possibly anomalous. Shifted frame: the corpus has a precedent for exactly this shape — the modulator suite's admission criteria produced **zero admissions after the founding three** (Vitality excluded, model-completeness excluded, the decay class excluded), and that emptiness was read as the criteria *working*. Under this lens, a species column that keeps coming back empty is the dissociation test biting as designed, and the species taxonomy's proxy value is untouched — the same way the modulator suite's value was untouched by its gate refusing everyone. Presentation-level: gives P7's "tidy sentence explicitly refused" criterion a positive framing instead of a bare refusal.

**LS-C (contrarian) — conditions under which the user's second horn is right.** Under what conditions would *"an artifact of the attention-species"* be the correct description? When the predication target is the **genus** read loosely as "the species-system": something derived from non-extractive attention *as such* is attention-dependent and species-indifferent. And the corpus has such an item in its own text: *"Any species of non-extractive attention produces `g(high)` … Supplication produces `g(low)`"* — the g-amplification is predicated on the genus, not on any species. Scenario 4's own resolution sentence (see the artifact-grounding catch in Phase 3) says exactly this: *"Both are non-extractive attention species — the genus property holds."* So the second horn, read at genus grain, has at least one true instance — which converts a34 (the genus recorded-untested) from a residue into a fourth axis with a worked first member.

### 2. Combination (Generator)

**CB-G (generic) — the sorting × the registry.** The forms_of_attachment finding already proposed a registry: *"for each named set in the theory, what it is a set of, what admits a member to it, and whether it is primitive or derived."* Combine it with this inquiry's three-axis sorting: the sorting is the registry's missing **predication column** for per-species claims — each attribution gets an address (*predicated on: species / genus / level / object / observer*). Neither alone has this: the registry proposal had no method for filling its "derived" column; the sorting had no home beyond this finding. Emergent: the finding's method plugs into an already-proposed artifact instead of floating free.

**CB-F (focused) — Scenario 4's "or" × the Oscillation Dynamics section.** The spec's own Oscillation Dynamics section (in `new_apt_layer.md`) says species oscillation within one person is normal and sustained purity is what misreads. Combined with Scenario 4's *"Dialogue-Focus … or Caregiver-Focus"*: the "or" admits an **oscillation reading** — one teacher alternating objects, each document freezing one phase. Under it the portability premise holds exactly when the teacher is in the Dialogue phase. Together with the readings already in play this makes the "or" **four ways ambiguous**: a fork (sensemaking's reading), a distributive-or over a plural class (the scenario says *"These teachers"* — some teachers one species, some the other), an oscillation (this candidate), and a genus-licensed indifference (the scenario's own next sentence — see Phase 3). All four leave the statement's premise conditional; they differ on what the condition *is*. Feeds P4 as an added criterion rather than overturning it.

**CB-C (contrarian) — the proxy × the detection layer.** Combine the species-as-proxy verdict with PRAGMA's operational reality: the detection catalog is species-shaped (display signals), and no detector exists for an object's generativity or an observer's expertise. Even though the bearer is `object × observer`, **the species is the only coordinate the instrument currently reads.** Emergent bound on the finding: the sorting corrects *predication*, not *detection practice* — per-species formulations remain load-bearing at the instrument layer until object-side detection exists, so the finding must not read as "stop using species."

### 3. Inversion (Framer)

**IN-G (generic) — invert the proxy's direction, to system level.** Level 1: "the species is a proxy for the object" → "the object is a proxy for the species" — objects are individuated *via* species names (we know "shared inquiry" is an object because Dialogue-Focus names it). Component-level; invert again. Level 2: "neither is prior — *attention on [X]* is one two-sided naming act, and asking which side bears the property presupposes they can come apart." System-level statement: **predication is decidable only where the two sides actually come apart** — fork cases (one name, two objects), observer-variation (object fixed, outcome varies), receiver-position variation (the Caregiver→Charm *"others"*), and all-species comparison (genus candidates). Everywhere else species/object predication is underdetermined in principle because they co-vary perfectly. Fertile: this is a **traction map** for the census — it says where the test can bite and where it cannot.

**IN-F (focused) — invert "the class has no first member."** Opposite: the class has members already — **under the corpus's own settled sense of *artifact*.** The debunking sense (a spurious appearance produced by the method) has instances the corpus itself catalogued: *Mission-Focus amplifies Hope* (shown spurious by the leadership finding — an object-level fact mis-written as a species claim, still standing in two documents), and arguably iteration-3's Mission-into-Self collapse argument (found INVALID on object-completability). So `I2`'s answer **forks by sense**: under the *derivative* sense — no members, a pool and a test (SV6's answer); under the *debunking* sense — at least two members, and they are the corpus's own corrected claims. The two answers are complementary: the debunking members are precisely the claims the derivative-sense sorting re-addresses. This scopes F10's sense-independence to `I1` explicitly — `I1`'s verdict is sense-independent; `I2`'s answer is sense-dependent. RE-TEST TRIGGER routed in Phase 3.

**IN-C (contrarian) — invert "the two-clause criterion is the sorting's instrument."** Opposite: the criterion (generative + external) is itself local — derived from one failing case, a repair for the followership question, not a general sorting tool. Tested: the sorting's actual instrument is the **dissociation test** (P2); the two-clause criterion is used only for object-axis followership items. The inversion fails to overturn but yields an actionable clarification the finding should state: **two instruments, not one** — the dissociation test decides *predication* (general); the two-clause criterion decides *followership support* (local). Conflating them would stretch the criterion across items (character, oscillation) it was never built for.

### 4. Constraint Manipulation (Framer) — both directions

**CM-A (ADD, generic) — add "every sorted item carries a detection note."** Each axis-assignment gains a pointer to how it would be observed in PRAGMA signals. Enables the Process run to inherit observables. Cost: presses against the Meaning/Process line (a41) — resolved by keeping notes as pointers, not procedures. Modest; disposition DEFERRED to the Process run's opening.

**CM-A2 (ADD, focused) — add "the finding must name the missing term, and the term already exists."** Sensemaking's A1 already bans un-tagged *artifact*; sharpen to a positive constraint: supply the term for *real but derived*. And no coinage is needed — the forms_of_attachment finding already built it: **primitive versus derived**, with *"derived-but-predictive is the honest description, and it is a real status."* Import that pair. Open item 4 (the corpus has no term) discharges by import rather than neologism, and the import connects two findings.

**CM-R (REMOVE, contrarian) — remove "the census is out of scope."** What becomes possible: sort all ~two dozen items now. Killed against the committed layer plan (C1, A6's elimination, I11) — but the removal leaves a real residue: the finding can ship the **unsorted worklist** (the pool's items enumerated, axes blank) without running it. A worklist is reporting, not application; it stays Meaning-side and hands the Process run its input. ADD-direction and REMOVE-direction both recorded; no empty flags.

### 5. Absence Recognition (Generator) — both levels, bidirectional

**AR-P (patch-level, generic) — the observer has no column.** The axis-scheme is species / level / object, yet the committed bearer is `object × **observer**` — and two of the four worked items used **observer-side variation as their evidence** (expert/novice for peer-follower; third-party/cared-for for Caregiver→Charm). The sorting's own method uses an axis its scheme doesn't name. Patch: give the observer an explicit place — either a fourth column or a per-item annotation of *which argument-place varies*. Without it, the scheme under-represents its own strongest evidence.

**AR-R1 (redesign-level, what's missing, focused) — a claim-addressing convention.** Designed from scratch: every claim in the theory carries a predication address (which element, which axis, which side — sender/receiver). The corpus's recurring error-class is one family — claims filed at the wrong address (*Hope amplification* written species-level; Fear's belief statement and detector on opposite sides of the sender/receiver fork; *"low Self-Positioning"* read as magnitude when it means species). This inquiry supplies the address-set for one region (per-species claims); the registry (CB-G) supplies the ledger. The full convention exceeds this inquiry's scope — RESEARCH FRONTIER.

**AR-R2 (redesign-level, already-present-in-different-form, contrarian) — the practice pre-exists un-named.** What is the corpus already doing that this inquiry treats as absent? **Axis-sorting itself.** The leadership finding's first MUST — the attention-object column with generativity/externality marked — *is* an object-axis sort of the six species. The anger finding's four-coordinate table *is* a sorting of one phenomenon across component-axes. The absence is the *name and the connective tissue*, not the operation. Tempers P6: the class-question's machinery pre-exists in two findings; this inquiry names it and adds the membership test.

### 6. Domain Transfer (Generator) — native source included

**DT-N (native: psychometrics/measurement theory, generic) — indicator versus construct.** The corpus's two-way dissociation *is* discriminant validation run informally (the multitrait-multimethod logic). Import, vocabulary-only per corpus convention (the SDT / Bayesian / factor-analysis precedents): the species is an **indicator**; `object × observer` is the **construct**; the pool's error-class is **indicator reification** — treating the measure as the thing measured. Bonus for P5: in experimental science, *artifact* standardly means the debunking sense (an effect produced by the apparatus) — the corpus's settled sense is the scientific standard, and the statement's derivative sense is the outlier, which strengthens the case for tagging.

**DT-F (deliberately different: biology, focused) — the trait-level question, and the genus test.** Biology's discipline for "at what taxonomic level is this trait fixed?" answers by comparison across the clade: a trait shared by all species of the genus is genus-level (ancestral), not species-level. Imported as an operational test the dissociation test cannot easily supply: **any pool item true of all six species is genus-predicated, not species-predicated.** This gives the a34 genus axis its membership test — cheap, runnable at Meaning level — and its first member passes it: *any* species produces `g(high)`. Converges with LS-C from an unrelated ground.

**DT-C (deliberately different: instrumentation/optics, contrarian) — artifacts as calibration data.** Imaging fields treat artifacts as informative about the instrument (lens flare characterizes the lens). Transfer: the pool's mis-predicated claims characterize **the authoring instrument** — everything in the two April spec sections was written before the object vocabulary existed (the leadership finding coined the column in August), so per-species framing was the *only available address* at authoring time. The error-class is pre-paradigm addressing, not sloppiness; the sources date-split cleanly across the vocabulary shift (a27's provenance fact explained rather than merely carried). Feeds P6: the sorting is partly a **translation** from pre-object vocabulary into post-object vocabulary.

### 7. Extrapolation (Generator)

**EX-G (generic) — the relocation trend meets the unfired trigger.** Three relocations shipped (inexhaustibility → object; completeness-ceiling → object-side; leadership mechanism → Resonance); this inquiry runs the fourth candidate through a test instead of asserting it. Extend the trend: each relocation moves one item off the person/species axes, and Cluster 4's third reopening condition — *"adjacent mechanisms reveal themselves to be attention-pattern variants rather than independent phenomena"* — is being satisfied **piecewise**. Extrapolated endpoint: Cluster 4 fires not by argument but **by census** — the trigger's determination procedure *is* the full sort, run over the pool plus the nine adjacent mechanisms the trigger itself names. Actionable now: route the nine adjacent mechanisms into the census worklist and state in P8 that the census is the trigger's test. The asymptotic prediction (fire-by-census) is DEFERRED on the census running.

**EX-F (focused) — the reconstruction tax compounds.** The spec/findings divergence (SP6, a27) grows with every finding; this inquiry needed five priors plus two spec sections that no document connects. Extend: each future inquiry pays a rising reconstruction tax; at some inquiry-count a consolidation run becomes cheaper than the next content run. The migration debt is known; the compounding framing and the crossover prediction are the new content. DEFERRED with trigger: re-examine when the next inquiry's Synthesis Trigger lists six or more priors.

**EX-C (contrarian) — extrapolate the test's streak.** The dissociation test is now 4-for-4 independent (three priors plus peer/follower). If every run returns independent, the test carries no information (a gate that admits no one and refuses no one differently). Two readings: the elements really are independent (they were admitted through orthogonality gates — plausible), or the test has only been run on pre-screened candidates (selection). The census is the first non-selected application — **its results calibrate the test itself**. Actionable honesty note for P7: if the census returns uniformly, suspect sensitivity before celebrating; a mixed return would validate both the test and the sample.

---

## Piece-Level Inversion Compliance (Production-task mode)

**Classification.** P1 — content-production (its outputs are verdicts flowing through I2/I4, not frames/labels/vocabulary/criteria/shapes; retrospective audit at run-end confirmed nothing downstream operates *under* P1 beyond consuming its conclusion). P2 — meta-decision (iv: commits the membership test P7 judges by). P3 — meta-decision (ii: species-as-proxy frame; I6 says P7 operates under it). P4 — meta-decision (ii: the "silent, not wrong" characterization that downstream relationship-framing depends on). P5 — meta-decision (iii: the two-senses vocabulary the finding then applies to its own word use). P6 — meta-decision (ii: "enumerating just is auditing" reframe P7 operates under). P7 — meta-decision (ii: the sample-not-census stance the finding's claims operate under). P8 — meta-decision (v: commits DO-NOTHING for three items, load-bearing for downstream routing).

**P2-inv.** Assumption: the dissociation test is *the* membership instrument for the whole pool. Reversed: different axes need different instruments. What follows: a **battery** — dissociation (species-vs-object, at traction points per IN-G), the all-species check (genus, per DT-F), duration/purity variation (level). Tested: survives — the four worked items already used three different evidence-types without naming them. Disposition: REFINE P7's method statement into the battery. *(Compliance: satisfied.)*

**P3-inv.** = IN-G (the proxy inverted to system level). Survives as the traction map. *(Satisfied — cross-referenced, not duplicated.)*

**P4-inv.** Assumption: the leadership finding's resolution of the "or" was *silent* — an undeclared pick. Reversed: the choice was **forced by the explanandum** — the leadership finding needed the teacher case to exhibit followership-support; only the Dialogue branch supports followership (the Caregiver branch yields devotion-without-followers by the criterion's own worked case); so the selection was entailed, left implicit rather than arbitrary. Tested: survives and composes with the artifact-grounding catch (Scenario 4's "or" was genus-licensed for *its* claim; the license doesn't transfer to object-level claims). F6's conditionality stands; the condition's character upgrades from "arbitrary undeclared" to "motivated but unstated." Disposition: REFINE P4's fifth criterion. *(Satisfied.)*

**P5-inv.** = IN-F. Sense-independence scoped to `I1`; `I2` forks by sense. *(Satisfied — cross-referenced.)*

**P6-inv.** = IN-F + AR-R2 jointly: under the debunking sense the class has members (the catalogued corrections), and the sorting practice pre-exists un-named. *(Satisfied — cross-referenced.)*

**P7-inv.** Assumption: four items is the honest sample size — claim no more, no less. Reversed both ways. *Claim more*: run the census now — killed on the layer commitment (see CM-R), residue = ship the worklist. *Claim less*: even four over-claims, because the items were salience-selected by the analysis. Tested: partially mitigated — the four come from three unrelated sources, and two sorted onto an axis (level) the analysis was not seeking, which a foregone conclusion would not produce; the selection caveat survives as one sentence in P7. Disposition: REFINE (add the selection caveat; add the worklist). *(Satisfied.)*

**P8-inv (property v — intervention-shape axis).** Committed shape: **DO-NOTHING** with reasons, for three items.
- *a40 (uncorrected Hope claims).* Alternative shape: **REPAIR** (correct the amplification table and Scenario 9 now). What follows: a spec edit — a Structural act this inquiry's Layer Commitment excludes, and one the never_named_layer finding shows is blocked on the unperformed migration. REPAIR killed on bounds; residue survives: P8's entry should name the executable shape (two one-line corrections, locations named) so the eventual actor inherits an edit, not a re-derivation.
- *a39 (the unfired trigger).* Alternative shape: **ADD-TEST** (fire the trigger's test now, inside the census). What follows: the nine adjacent mechanisms join the census worklist, and P8 states that the census *is* the trigger's determination procedure (EX-G). Survives: DO-NOTHING upgrades to record-and-route. REFINE.
- *a34 (the genus untested).* Alternative shape: **ADD-DIMENSION** (promote the genus to a fourth axis now, with DT-F's all-species check as its test and the `g(high)` line as its first member). What follows: the axis-scheme gains a member cheaply at Meaning level; consistent with sensemaking's own Frame-exit re-location of the genus. Survives. REFINE — the strongest of the three shape-inversions.
*(Compliance: satisfied — shape named, alternatives named, consequences stated, all tested below.)*

---

## Inherited Frame Audit

**Seed-level central assumptions (multi-assumption fallback applied):** (1) *the bearer is `object × observer` and the species is a proxy* — challenged explicitly by IN-G (inverts the proxy's direction to a system-level statement). (2) *the class has no first member* — challenged by IN-F (states the opposite under the corpus's settled sense). (3) *both horns fail* — challenged by LS-C (constructs the genus-grain conditions under which the second horn holds).

**Piece-level load-bearing commitments:** P2's test-as-criterion — challenged (P2-inv battery; EX-C sensitivity). P3's proxy frame — challenged (IN-G). P4's "silent" — challenged (P4-inv "forced by explanandum"; CB-F's alternative readings). P5's sense-independence — challenged (IN-F scopes it). P6's no-first-member reframe — challenged (IN-F; AR-R2). P7's sample stance — challenged both directions (P7-inv). P8's DO-NOTHING ×3 — challenged on the shape axis (P8-inv).

**Firing condition: NOT FIRED.** Every seed-level assumption and every piece-level commitment has at least one explicit challenge in the candidate set. Proceed to Phase 3.

---

## Phase 3 — Test

### Artifact-grounding catch (6th test) — the run's most consequential single result

Checking Scenario 4's quoted "or" against its source in full (`apt_layer_proof_scenarios.md`, Scenario 4) finds that **the very next sentence adjudicates the indifference**: *"These teachers operate in **Dialogue-Focus** (attention on the shared inquiry) or **Caregiver-Focus** (attention on student's development without harvesting student approval). **Both are non-extractive attention species — the genus property holds.**"*

Three consequences, each routed as a RE-TEST TRIGGER on a committed claim:

1. **The "as though the choice did not matter" framing (SV6, Internal-Consistency) softens.** Scenario 4's "or" is not an oversight — it is **genus-licensed for the claim Scenario 4 was making** (devotion via multiplicative gating, which any non-extractive species produces). The choice genuinely did not matter *for that claim*. It matters completely for the leadership finding's object-level claim — and the license did not transfer. Re-test outcome: the corpus-contradicts-itself framing revises to a **level-shift**: two documents asking questions at two predication levels, each correctly indifferent/sensitive to the species choice at its own level. The proxy-failure claim (F5) survives — one name still covers two objects, and it still bites at object-level questions — but the finding should present Scenario 4 as *correct at its own level*, echoing the anger finding's translation-not-repair precedent.
2. **"These teachers" is plural** — the "or" admits a distributive reading over a class of teachers, alongside the fork, oscillation (CB-F), and genus-license readings. Four readings; all leave the portability premise conditional; the finding should name the set rather than adjudicate.
3. **The genus axis's standing improves from proposed to evidenced.** Scenario 4's own explanation is a genus-predicated claim sitting in the pool — a second worked member for the axis alongside the `g(high)` line, discovered by reading a quote to its end (the corpus's own audit precedent, now with a second confirmed instance).

This catch is itself an instance of the corpus's recorded failure mode (*load-bearing citation read short* — the coherence finding's mid-stage error, and its COULD item proposing exactly this audit). That the same mode fired against this inquiry's own upstream discipline strengthens the case for the audit habit.

### 5-test cycle and dispositions

| Candidate | Novelty | Scrutiny survival (strongest objection → outcome) | Fertility | Actionable | Mech. independence | Disposition |
|---|---|---|---|---|---|---|
| LS-G instrument-frame | mod | "just presentation" → no: names the deliverable's reusable part | high | yes | converges w/ CB-G | **ACTIONABLE** |
| LS-F emptiness-as-biting | mod | "false analogy" → holds: same gate-shape, same corpus | mod | yes | independent ground (modulator suite) | **ACTIONABLE** (presentation) |
| LS-C genus-grain horn 2 | high | "not what the statement says" → charitable reading, flagged as such | high | yes | converges w/ DT-F, Scenario 4 catch | **ACTIONABLE** (via Assembly C) |
| CB-G sorting × registry | mod | "registry is another finding's item" → connection is the content | high | yes | converges w/ LS-G, CM-A2 | **ACTIONABLE** |
| CB-F oscillation reading | high | "Scenario 4 describes types, not one oscillator" → both live; joins a four-reading set | mod | yes | independent (spec's own section) | **ACTIONABLE** (P4 criterion) |
| CB-C detection-layer bound | mod | "PRAGMA could read objects" → could, but no detector exists; softened claim holds | mod | yes | single-mechanism | **ACTIONABLE** (caveat) |
| IN-G traction map | high | "underdetermination overstated" → holds where co-variation is perfect, which is the default case | high | yes | converges w/ P2-inv | **ACTIONABLE** |
| IN-F sense-forked `I2` | high | "debunking members are just errors, not a class" → the sense's own definition makes shown-spurious claims its members | high | yes | converges w/ DT-C, DT-N | **ACTIONABLE** + RE-TEST below |
| IN-C two-instruments note | mod | none survived against it | mod | yes | single | **ACTIONABLE** (one para) |
| CM-A detection notes | low | Meaning/Process line pressure | mod | yes | single | **DEFERRED** (trigger: Process run opens) |
| CM-A2 import primitive/derived | mod | "import ≠ the same distinction" → forms finding's usage matches exactly (derived-but-predictive) | mod | yes | converges w/ DT-N | **ACTIONABLE** |
| CM-R worklist residue | mod | layer commitment → residue only survives | high | yes | converges w/ EX-G, P8-inv-a39 | **ACTIONABLE** |
| AR-P observer column | high | "SP1 already folds it in" → folding hides the method's own evidence-type | mod | yes | converges w/ IN-G | **ACTIONABLE** |
| AR-R1 addressing convention | high | scope | high | not per-inquiry | single | **RESEARCH FRONTIER** |
| AR-R2 practice pre-exists | mod | none | mod | yes | converges w/ CB-G | **ACTIONABLE** (one para) |
| DT-N indicator/construct | mod | apparatus-adoption risk → vocabulary-only, per three precedents | mod | yes | converges w/ CM-A2 | **ACTIONABLE** (anchor) |
| DT-F all-species genus test | high | "six species too few for the inference" → at Meaning level the check reports predication, not truth (C10 bounds it) | high | yes | converges w/ LS-C | **ACTIONABLE** |
| DT-C errors as calibration | high | "excuses the errors" → dates them; the corrections still stand | mod | yes | converges w/ IN-F | **ACTIONABLE** |
| EX-G census = trigger's test | high | "census may not decide Cluster 4" → it operationalizes the trigger's own wording | high | yes | converges w/ CM-R, P8-inv | **ACTIONABLE** (routing) / prediction **DEFERRED** |
| EX-F compounding tax | low-mod | known debt | mod | not now | single | **DEFERRED** (trigger: ≥6-prior inquiry) |
| EX-C test-sensitivity note | mod | "elements really are independent" → both readings preserved; census decides | mod | yes | single | **ACTIONABLE** (one sentence) |
| P2-inv battery | mod | — | high | yes | converges w/ IN-G, DT-F | **ACTIONABLE** |
| P4-inv forced-by-explanandum | high | "still undeclared" → yes; character of the condition upgrades, conditionality stands | mod | yes | converges w/ grounding catch | **ACTIONABLE** |
| P7-inv selection caveat | mod | mitigated by source- and outcome-diversity | low | yes | single | **ACTIONABLE** (one sentence) |
| P8-inv-a40 executable shape | mod | bounds → residue survives | mod | yes | single | **ACTIONABLE** (residue) |
| P8-inv-a39 route to census | mod | — | high | yes | converges w/ EX-G | **ACTIONABLE** |
| P8-inv-a34 genus as 4th axis | high | "axis with 1–2 members" → membership test + two candidates is more than the object axis had before August | high | yes | converges w/ DT-F, LS-C, catch | **ACTIONABLE** |

### RE-TEST TRIGGER executions (committed claims touched by survivors)

- **F7 / "class has no first member" (touched by IN-F).** Re-tested: F7 was stated — and remains true — for the *derivative* sense (nothing has ever been *called* an artifact-of; the dissociation test never returned a positive). Under the *debunking* sense the class is non-empty by the sense's own definition. Resolution: F7 survives **scoped to the derivative sense**; SV6's `I2` paragraph gains the fork. No contradiction — a scope-annotation.
- **F10 / "verdict holds under either sense" (adjacent).** Re-tested: F10 is about `I1` and stands. The finding must state the scope: sense-independence is `I1`'s property, not the inquiry's.
- **F5–F6 / Scenario 4 framing (touched by the grounding catch + P4-inv).** Re-tested above: proxy-failure and conditionality stand; the "corpus contradicting itself" and "silent" characterizations soften to level-shift and motivated-but-unstated respectively.
- **SP1 / three axes (touched by P8-inv-a34 + AR-P).** Re-tested: sensemaking itself re-located the genus in (Frame-exit §4) and folded the observer into the object axis (SP1's own wording). Promoting genus and unfolding observer are consistent extensions, not contradictions — the scheme becomes **species / genus / level / object / observer**, with per-axis evidence-types (the battery).

### Shared-input detection (Mechanism Independence refinement)

The three convergence clusters were checked for shared upstream inputs. *Census package* (CM-R, EX-G, P8-inv-a39, DT-F, IN-G): grounds are respectively the layer commitment, the relocation trend, the shape vocabulary, a biology transfer, and the proxy inversion — independent. *Sense-fork* (IN-F, DT-C, DT-N): grounds are the P6 inversion, an instrumentation transfer, and psychometrics — independent. *Axis instrumentation* (AR-P, IN-G, DT-F, P2-inv, IN-C): AR-P and IN-G both lean on the worked items' evidence-types — partially shared; the battery's genus instrument (DT-F) is independent, so the cluster survives with one internal dependency noted. No spurious convergence found.

---

## Assembly Check

**Assembly A — the census package (emergent, strongest).** CM-R's worklist + P8-inv-a39's nine adjacent mechanisms + P2-inv's instrument battery + IN-G's traction map + DT-F's genus test + EX-C's calibration note + EX-G's stake. Together: the Process run's **full specification exists at Meaning level** — what to sort (pool + nine mechanisms), with what (three instruments matched to axes), where the tests bite (traction map), what hangs on it (Cluster 4's own determination procedure), and how to read a uniform result (sensitivity check). No individual candidate has this; "census deferred" becomes "census specified."

**Assembly B — the sense-forked answer to `I2` (emergent).** IN-F + DT-C + P6's pool-and-test + CM-A2/DT-N's term import. `I2`'s answer becomes one structure with two faces: *derivative sense* — no members yet; a pool, a test, a worked sample, and the imported primitive/derived vocabulary; *debunking sense* — members exist, they are the corpus's own catalogued corrections, and they date-split at the August vocabulary shift (pre-paradigm addressing, not sloppiness). Complementary, not rival: the debunking members are the claims the derivative sorting re-addresses.

**Assembly C — the instrumented five-axis scheme (emergent).** P8-inv-a34 (genus, with test and two candidate members) + AR-P (observer unfolded) + IN-C (instruments not conflated) + P2-inv (battery). The sorting stops being three-labels-plus-judgment: **species / genus / level / object / observer**, each with its evidence-type — dissociation-at-traction-points (species vs object), all-species check (genus), duration/purity variation (level), object-fixed observer-variation (observer).

**Axis coverage check.** The candidate set varies along: content (what the finding says), method (how the sorting runs), presentation (how the verdict reads), scope (what may be claimed), intervention-shape (P8's three), and relationship-to-priors (P4-inv, the grounding catch). No relevant axis without a variant. Per-element mechanism-trace: every P1–P8 piece received mechanism work (P1 via LS-C challenging its horn at genus grain; P2 via P2-inv/EX-C; P3 via IN-G; P4 via CB-F/P4-inv/catch; P5 via IN-F/DT-N; P6 via IN-F/AR-R2/DT-C; P7 via P7-inv/CM-R/EX-C/AR-P; P8 via three shape-inversions). No silent rows.

---

## Telemetry

- **Generators applied: 4 / 4** (Combination, Absence Recognition, Domain Transfer, Extrapolation). **Framers applied: 3 / 3** (Lens Shifting, Constraint Manipulation, Inversion). Full coverage.
- **Outputs: 21 mechanism variations** (7 × 3; CM's both-directions covered as ADD ×2 + REMOVE ×1; AR's both-levels covered as patch ×1 + redesign ×2 bidirectional) **+ 6 distinct piece-level Inversion outputs** (P2-inv, P4-inv, P7-inv, P8-inv ×3; P3/P5/P6-inv cross-reference mechanism outputs) **+ 1 artifact-grounding catch = 28 candidate-bearing outputs.**
- **Convergence: YES — three clusters** (census package: 5 mechanisms; sense-fork: 3; axis instrumentation: 5 with one internal dependency noted). Confidence high on all three.
- **Survivors tested: 28 / 28** through the 5-test cycle (+ artifact-grounding where categorical claims about project state were made; 4 RE-TEST TRIGGERs executed on committed claims, all resolving as scope-annotations or characterization-softenings, none as contradictions).
- **Dispositions (counted from the table): 24 of 27 rows ACTIONABLE** — two of them as residues of killed primaries (CM-R's removal proper and P8-inv-a40's REPAIR were killed; their residues carry the action) — **· 2 rows DEFERRED with triggers (CM-A, EX-F) plus EX-G's prediction sub-item DEFERRED · 1 RESEARCH FRONTIER (AR-R1) · the grounding catch executed in-run as 4 RE-TEST TRIGGERs.**
- **Per-piece classification:** P1 content-production (retro-audit run: confirmed); P2–P8 meta-decision; **piece-level Inversion compliance: satisfied ×7, violated ×0, overridden ×0**; P8 property-(v) axis-distribution: `P8: [Inversion:intervention-shape ×3]` — aligned, no override needed.
- **Failure modes: none observed.** Premature evaluation — generation and testing separated. Single-mechanism trap — 7/7. Early frame lock — contrarian variation mandatory per mechanism; the frame audit ran. Innovation without grounding — 5-test cycle + artifact-grounding executed (and produced the run's biggest catch). Mechanism exhaustion — 22 actionable survivors. Survival bias — the uncomfortable outputs (IN-F's fork of a committed claim, EX-C's test-sensitivity doubt, P7-inv's claim-less direction, the softening of two of sensemaking's own framings) were retained, not killed.
- **Overall: PROCEED.**
