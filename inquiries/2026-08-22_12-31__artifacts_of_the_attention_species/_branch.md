# Branch: artifacts_of_the_attention_species

## Source Input

```text
a property of the person — definable on its own, portable from the teacher to the leader — or an artifact of the attention-species? 

what other artifacts exists?  


lets dive deep into this one now
```

## Articulation Reference

- **File:** `inquiries/2026-08-22_12-31__artifacts_of_the_attention_species/articulate_simple.md`
- **Itemize count:** 2
- **Per-item identifiers:** `I1`, `I2`
- **Verdict:** **HIGH-FLAG**
- **Flagged condition:** **`I1` names no subject, and every downstream discipline depends on one.** The item asks whether something is *a property of the person* or *an artifact of the attention-species* and never says what that something is — there is no noun in the sentence for the thing being classified. Flagged at HIGH confidence rather than MED because the absence is verifiable by reading the sentence and because it is **binding**: the two horns cannot be weighed, the two criteria cannot be applied, and the two named cases cannot be compared until it is fixed. Whatever fixes it comes from outside the statement, and the downstream framing must carry that rather than resolve it silently.
- **Further conditions carried without rising to flags:**
  - **`I2` inherits the same gap through one word.** *"What **other** artifacts exists?"* depends on a referent supplied outside its own text, and additionally presupposes both that the class exists and that it has more than one member.
  - **The two named criteria have three readings.** *"Definable on its own"* and *"portable from the teacher to the leader"* may be two independent tests, one test stated twice, or a test and its evidence.
  - **The second horn names a different kind of thing from the first.** *A property of the person* names a **bearer**; *an artifact of the attention-species* names a **by-product relation**.
  - **`I1` is a sentence fragment continuing something not supplied.** It opens lowercase with a bare noun phrase and no verb.

## Question

### Item `I1`

**Literal statement (MultiDepth, uncontaminated):**

> *"a property of the person — definable on its own, portable from the teacher to the leader — or an artifact of the attention-species?"*

A sentence fragment posing a disjunction. It opens with a bare noun phrase, carries no verb, and names no subject. Between em-dashes it supplies two qualifications of the first horn — *definable on its own* and *portable from the teacher to the leader*. The second horn, after *"or"*, names a different kind of thing: not a different bearer but a **by-product of something else** — *an artifact of the attention-species*.

**What kinds of ask the statement carries (MQ1, verdict-axis — preserved as ambiguities):**

- `adjudicate-the-disjunction` — decide which of the two horns holds, with reasons.
- `identify-the-subject` — **the statement names no subject.** Establishing what is being classified may itself be the ask.
- `test-a-prior-relocation` — *"or an artifact of the attention-species"* reads as a doubt raised against something previously located in the person; the ask may be to check whether that location holds.
- `dissolve-the-disjunction` — the two horns may not be exclusive, and the ask may be to show the question cannot be answered as posed.
- `define-the-property` — *"definable on its own"* states a criterion; the ask may be to attempt the standalone definition and let the attempt settle it.
- `test-portability` — *"portable from the teacher to the leader"* states a second criterion; the ask may be to check whether the thing survives transfer between those two cases.

**What action-endpoints are plausible (MQ3, intent-axis WHAT — preserved as ambiguities):**

- `produce-a-verdict` — one horn selected, with the reasoning.
- `produce-a-dissolution` — a demonstration that the disjunction is false and the question malformed.
- `produce-a-definition` — the standalone definition attempted, with its success or failure as the result.
- `produce-a-portability-test` — the transfer between the two named cases run, and property-hood read off the outcome.
- `produce-a-status-report` — what is currently claimed about this, where, and how firmly.
- `produce-a-criterion` — a stated test for what makes anything an artifact-of rather than a property, applicable beyond this case.

**MQA outcomes (carried through, not collapsed):**

- **Reconcile 1 — joint axis `what is being classified`:** folds MQ1's `identify-the-subject`, MQ2's verdict sub-axis on the missing noun, and MQ4's missing-subject item. The item's entire answerability rests on one unstated referent, and every other ambiguity in this bundle is conditional on it. **Nothing in the statement fixes it.**
- **Reconcile 2 — joint axis `what the two named criteria do`:** folds MQ1's `define-the-property` and `test-portability`, MQ2's verdict sub-axis on one-criterion-or-two, MQ3's `produce-a-definition` and `produce-a-portability-test`, and MQ4's binding-criteria item. The three readings — two independent tests, one test stated twice, or a test and its evidence — put different work in front of the answer.
- **Surface — irreducible overlap:** MQ1's `adjudicate-the-disjunction` and `dissolve-the-disjunction`. Answering the question **as posed** and refusing **its form** are different acts with different outputs, and the second does not presuppose that either horn is right.

### Item `I2`

**Literal statement (MultiDepth, uncontaminated):**

> *"what other artifacts exists?"*

A short interrogative asking for the remaining members of a class. *"Other"* excludes one already-identified member, which the sentence does not name. *"Artifacts"* is used without a definition.

**What kinds of ask the statement carries (MQ1, verdict-axis — preserved as ambiguities):**

- `enumerate-the-class` — list the other artifacts.
- `establish-the-class-exists` — *"other artifacts"* presupposes at least one; whether the class has members at all may be what is being asked.
- `define-the-class` — before anything can be enumerated, what makes something a member has to be said, and the statement does not say it.
- `audit-for-mis-filed-items` — the ask may be to find things currently treated one way that are really artifacts, which is a search rather than a listing.
- `bound-the-class` — say how many there could be, or whether the class is closed, rather than which ones there are.

**What action-endpoints are plausible (MQ3, intent-axis WHAT — preserved as ambiguities):**

- `produce-an-enumeration` — the list itself.
- `produce-a-membership-criterion` — the test that would generate the list.
- `produce-an-audit` — a sweep of existing claims for ones that are mis-filed.
- `produce-a-completeness-claim` — a statement about whether the list is all of them.
- `produce-an-explicit-empty` — the finding that there are none, stated as a result rather than as an absence.

**MQA outcomes (carried through, not collapsed):**

- **Reconcile — joint axis `does the class exist before it is enumerated`:** folds MQ1's `establish-the-class-exists` and `define-the-class`, MQ2's verdict sub-axis on whether the class is inherited or proposed, MQ3's `produce-a-membership-criterion` and `produce-an-explicit-empty`, and MQ4's zero-is-an-answer item. **The statement asks for the members and supplies neither the criterion nor the confirmation that there are any.**
- **Surface — irreducible overlap:** MQ1's `enumerate-the-class` and `audit-for-mis-filed-items`. Listing what is already known to be there and searching for what has been filed under the wrong heading are different operations over the same material, and the second may return things the first would not.

## Goal

### Item `I1`

**Deliverable shape (Deconstruct tuple):**

- **deliverable:** a classification verdict on a subject the statement does not name — which requires first establishing what the subject is.
- **kinds:** a verdict · a dissolution · a standalone definition · a portability test · a status report · a stated criterion.
- **bounds:** the two horns (*a property of the person* / *an artifact of the attention-species*) · the two named criteria (*definable on its own*; *portable from the teacher to the leader*) · the teacher case and the leader case · *"the attention-species"* · *"the person"*.

**What motivations a good answer might serve (MultiDepth WHY-axis — preserved as ambiguities):**

- `validation-driven` — the second horn reads as a doubt raised against a position, and the motive may be to see whether it holds.
- `parsimony-driven` — if the thing is an artifact, the theory carries one fewer independent item; the motive may be to remove something.
- `definition-driven` — *"definable on its own"* is a demand for a definition that stands without leaning on anything, and the motive may be to obtain one or to establish that none is available.
- `generalisation-driven` — *"portable from the teacher to the leader"* is a demand that the thing work in more than one case, and the motive may be reach rather than correctness.
- `architecture-driven` — *property* versus *artifact-of* is a claim about what kinds of thing exist, so the motive may be about the shape of the whole rather than about this one item.
- `curiosity-driven` — *"lets dive deep"* names no outcome and attaches to no deliverable.
- `application-driven` — a portable property is usable anywhere; an artifact of a species is only readable where that species is present, and the difference determines where the thing can be used.

**Context downstream needs that isn't in the statement (MQ2 — preserved as ambiguities):**

- **verdict sub-axis:** **what the subject is** — no noun is supplied and nothing downstream can proceed without it · **what *"the teacher"* and *"the leader"* refer to** — they read as two specific cases rather than generic roles · **what *"the attention-species"* names** — an established term with a definition, or a compound formed for this sentence · **whether *"definable on its own"* and *"portable"* are two criteria or one stated twice** · **whether the question has already been answered somewhere**, and if so which way, since the phrasing suggests a position being revisited rather than formed.
- **kinds sub-axis:** whatever prior treatment exists of the unnamed subject · the teacher case and the leader case in whatever source states them · what species of attention are recognised, and by what enumeration · what an *artifact-of* relation consists of — what makes something an artifact of a thing rather than a property in its own right · any existing distinction between *a property* and *an artifact of* in this material · what *"the person"* is being contrasted with, given that the second horn names a species rather than a different bearer.
- **stance sub-axis:** **whether the answer may reject both horns** or must pick one · **whether the second horn is the user's hypothesis or an evenly-weighted alternative** · **whether this is a Meaning act** (what kind of thing the subject is), **a Structural act** (where it should sit), **or a Process act** (how one would tell in a given case) · **whether "the person" means the sender, the receiver, or either** — the phrase is neutral and the two readings put the property on opposite sides.

**What would explicitly fail (MQ4 boundary-axis — the NOT-list):**

- **Ambiguous-whether-excluded — the missing subject.** By naming no subject, the statement leaves open whether answering about the wrong one would count as answering at all. This is the item's binding constraint and it is stated nowhere.
- **Ambiguous-whether-excluded — whether *both* or *neither* is admissible.** A disjunctive question conventionally invites one of two answers; nothing here says a third is allowed or forbidden.
- **Ambiguous-whether-excluded — whether the two criteria are binding.** They may be the tests the answer must run, or descriptive gloss on what a property would be.
- **Recorded observation — the statement is a sentence fragment.** It opens lowercase with a noun phrase and carries no verb; it reads as the continuation of something not supplied, and what it continues is not in the text.

### Item `I2`

**Deliverable shape (Deconstruct tuple):**

- **deliverable:** an enumeration of a class whose membership criterion the statement does not supply — which requires first establishing what the class is and whether it has members.
- **kinds:** a list · a membership criterion · an audit · a completeness claim · an explicit empty.
- **bounds:** artifacts of attention-species, *other than* the one at issue in the preceding question.

**What motivations a good answer might serve (MultiDepth WHY-axis — preserved as ambiguities):**

- `completeness-driven` — if one exists, the set should be known.
- `pattern-driven` — a second instance would make the first a pattern rather than a one-off, and the motive may be to find out which it is.
- `parsimony-driven` — each artifact found is something that need not be carried as an independent item.
- `architecture-driven` — the class's existence is a claim about the shape of the whole, not about any of its members.
- `validation-driven` — an enumeration that came up empty would cast doubt back on the first artifact.
- `curiosity-driven` — no outcome is named.

**Context downstream needs that isn't in the statement (MQ2 — preserved as ambiguities):**

- **verdict sub-axis:** **whether any artifact has been identified at all** — the word *other* inherits its referent from outside this item's text · **whether the class already exists** or is being proposed by the question · **whether *artifacts* here means the same thing it means where the word was introduced** · **whether the enumeration ranges over one attention-species or all of them** · **whether the answer is expected to be non-empty**.
- **kinds sub-axis:** whatever species of attention are recognised, and their enumeration · the properties currently attributed to persons in the relevant material · what the *artifact-of* relation consists of · any existing enumeration of a comparable class, as a model for what this one should look like · the case that established the first artifact, since it is the only worked instance available.
- **stance sub-axis:** **whether an empty answer is acceptable** — reporting *none* as a result rather than as a failure · **whether candidates may be proposed** or only existing ones reported · **whether the enumeration is over what is claimed** or over the phenomenon itself · **whether completeness is expected**, or a partial list suffices.

**What would explicitly fail (MQ4 boundary-axis — the NOT-list):**

- **Ambiguous-whether-excluded — whether zero is an answer.** The question presupposes there are some; whether *none* would count as answering is not said.
- **Ambiguous-whether-excluded — whether proposing counts.** The question asks what exists; whether a candidate not previously identified may be offered is undetermined.
- **Recorded observation — *"other"* is load-bearing and inherited.** The word makes this item depend on a referent supplied outside its own text. What the first artifact is, is not in this item.

## Considered Articulations

**Item `I1` — *"a property of the person — definable on its own, portable from the teacher to the leader — or an artifact of the attention-species?"*:**

1. *"Decide whether the thing in question is a property in its own right or an artifact of the attention-species it appears in."*
2. *"Establish what the statement is asking about — it names no subject — and classify that."*
3. *"Attempt the standalone definition, and treat the attempt's success or failure as the verdict."*
4. *"Test whether the thing survives transfer from the teacher case to the leader case, and read property-hood off the outcome."*
5. *"Show that being a property and being an artifact-of are not exclusive, so the disjunction as posed cannot be answered."*
6. *"Say what would make anything an artifact of an attention-species rather than a property in its own right, and then apply the test."*

**Item `I2` — *"what other artifacts exists?"*:**

1. *"List the other things currently treated as properties that are actually artifacts of an attention-species."*
2. *"Say what makes something an artifact of an attention-species, then apply that test across the material."*
3. *"Report whether any second artifact exists at all, allowing for the answer to be none."*
4. *"Enumerate the class across every attention-species recognised, not only the one at issue."*
5. *"Audit the person-attributed properties for ones whose real bearer is what the attention is on."*

## Scope Check

**Item `I1` — Question covers goal.** IN-scope per Deconstruct's bounds: the two horns, the two named criteria, the teacher and leader cases, *"the attention-species"*, and *"the person"*. OUT-of-scope per MQ4: nothing is excluded outright — all four MQ4 entries are **ambiguous-whether-excluded** or **recorded observations**, and the binding one (the missing subject) is a constraint the statement imposes by omission rather than by exclusion.

**Item `I2` — Question covers goal.** IN-scope per Deconstruct's bounds: artifacts of attention-species other than the one at issue. OUT-of-scope per MQ4: again nothing outright; the two live items are whether *zero* is an admissible answer and whether proposing counts as reporting.

**One dependency recorded, not resolved.** `I2` depends on `I1` through the word *"other"* — it presupposes `I1`'s second horn holds for whatever `I1`'s subject is. That is a **presupposition, not an identity**, which is why the two are separate items. If `I1`'s verdict is that the subject is a property rather than an artifact, `I2` loses its anchor and would have to be re-read as *what artifacts exist at all*. Both readings of `I2` are preserved in its variant set (variant 3 allows the empty answer; variant 4 widens past the one at issue).

**Specific-vs-pattern check.** `I1` names one unnamed subject and two specific cases; `I2` asks about a class. **The statement itself spans both levels**, so the usual choice does not arise in the usual form — but a related one does. The teacher and the leader are two **specific cases**, and the question is whether they are *the whole test* (does the thing move between these two?) or *examples of a wider portability demand* (does the thing move between arbitrary cases?). **Both readings are live and the statement does not decide.** Per the default the broader pattern is addressed — a portability criterion that only ever ranges over two named cases is not a criterion — **and the narrow reading is retained**, because the two cases are the only ones the statement supplies and they may be the only two available.

## Layer Commitment

**Required.** The articulation's MQ1 carries `identify-the-subject` and `dissolve-the-disjunction`, and MQ2's stance sub-axis explicitly opens the Meaning-versus-Structural-versus-Process question. `I2` additionally asks for a class the statement does not define, which is a question about what kinds of thing the framework contains. Both items act on the framework's own element inventory, which is the trigger condition.

**Primary layer: MEANING.** The first question is *what kind of thing the subject is* — whether *property in its own right* and *artifact of a species* are two genuine kinds, what distinguishes them, and what the two named criteria test. Everything else depends on it: a class cannot be enumerated (`I2`) before its membership criterion is stated, and no placement or procedure can be specified for something whose kind is unsettled.

**Other layers considered and out of scope for THIS run:**

- **Structural** — where an artifact-of belongs in the write-up, and what changes if something moves from property to artifact. Out of scope because the shape is downstream of the kind; and because the immediately prior inquiry established that this material's Structural acts are blocked on an unperformed migration, which a Structural run here would inherit.
- **Process** — how an analyst determines, for a given case, whether they are looking at a property or an artifact. Out of scope because a determination procedure for a distinction that is not yet drawn would inherit that instability. Retained as `I1` considered-articulation 6 and `I2` variant 2, so the criterion question is not lost — **stating a criterion is Meaning work; operationalising it is Process work**, and the line is drawn there.

**Sequential plan.** Meaning first, in this run. If the meaning-layer verdict is that *artifact-of* is a real relation with a statable test, the next inquiry is **Process** — how to apply the test — because `I2` is an enumeration request and enumerating requires applying a criterion case by case. **Structural** follows only after that, and only if the enumeration returns members. This order inverts the usual Meaning→Structural→Process sequence deliberately: the deliverable here is a **class**, and a class needs a working test before it needs a place to live.

## Synthesis Trigger

**Required** — MQ2's verdict and kinds sub-axes on both items name two or more prior outputs as needed context: the source of the teacher and leader cases, whatever enumerates the attention-species, and whatever established the first artifact.

**Priors this inquiry consumes:**

- `inquiries/2026-08-21_18-37__leadership_and_inexhaustibility/finding.md` — the only place in the corpus where a property is **relocated from a person to what their attention is on**. Commits to: inexhaustibility being a property of the attention-object rather than of the person; the deep-teacher case; the leadership case; and *self-as-object* as a real subclass that survived the relocation.
- `inquiries/self_focus_generalization/finding.md` (iteration-3) — the source of the **species** vocabulary. Commits to: Non-Extractive Attention as a genus with named species (Self-Focus, Caregiver-Focus, Mission-Focus, Task-Focus and others); the two-level naming; and nine adjacent mechanisms scoped outside the theory.
- `inquiries/2026-08-22_10-54__model_completeness_fifth_category/finding.md` — the immediately prior finding. Commits to: model-completeness being a property of the receiver's model with its **ceiling set sender-side by the attention-object**; the residual practice and its four entries; and the finding that four things sit outside the formula with four different structural roles.
- `inquiries/2026-08-22_06-09__the_never_named_layer/finding.md` — commits to: **properties being had, not admitted**, and no gate being owed to a property; and the element-ontology as an open research frontier whose revival gate is met.
- `inquiries/apt_modulator_landscape/finding.md` (iteration-3.2) — commits to: the eight modulator entry criteria; the Vitality exclusion as the precedent for real-but-non-qualifying factors; and *"Different architectural category"* as the corpus's dismissal phrase.

**Inherited commitments requiring re-test (not assumption):**

1. **A property can be relocated from a person to their attention-object.** This is the move `I1`'s second horn generalises. Whether the relocation was a one-off adjustment to one case or an instance of a repeatable relation is exactly what `I2` asks, and it has never been tested twice.
2. **The species enumeration is fixed and complete.** `I2` cannot range over "the attention-species" unless there is a settled set of them. Whether the corpus's species list is closed, and whether *artifact-of-a-species* even presupposes a closed list, needs checking rather than assuming.
3. **Properties are had, not admitted, and owe no gate.** If that holds, then an *artifact of a species* is not a category with members either — which would make `I2`'s enumeration request structurally similar to a question the immediately prior finding answered, and the answer may or may not transfer.
4. **Model-completeness's ceiling is sender-side while the quantity is receiver-side.** That two-sidedness is the closest existing analogue to the property/artifact split `I1` proposes, and whether it is the same distinction under two names or a different one is undetermined.
5. **The teacher and leader cases are the corpus's own and say what the statement implies they say.** *"Portable from the teacher to the leader"* attributes a transfer to two cases; whether those cases were ever presented as a portability test, or as something else, is checkable.

Sensemaking and Critique must be planned to actually perform this re-testing, not merely record the inheritance.
